# 15.三数之和（中等）

## 题目描述

给你一个整数数组 `nums` ，判断是否存在三元组 `[nums[i], nums[j], nums[k]]` 满足 `i != j`、`i != k` 且 `j != k` ，同时还满足 `nums[i] + nums[j] + nums[k] == 0` 。请你返回所有和为 `0` 且不重复的三元组。

**注意：**答案中不可以包含重复的三元组。

 

 

**示例 1：**

```
输入：nums = [-1,0,1,2,-1,-4]
输出：[[-1,-1,2],[-1,0,1]]
解释：
nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0 。
nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0 。
nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0 。
不同的三元组是 [-1,0,1] 和 [-1,-1,2] 。
注意，输出的顺序和三元组的顺序并不重要。
```

**示例 2：**

```
输入：nums = [0,1,1]
输出：[]
解释：唯一可能的三元组和不为 0 。
```

**示例 3：**

```
输入：nums = [0,0,0]
输出：[[0,0,0]]
解释：唯一可能的三元组和为 0 。
```

 

**提示：**

- `3 <= nums.length <= 3000`
- `-105 <= nums[i] <= 105`

## 我的Python解答

最开始最直接的想法是三层遍历，但是这样先不考虑时间的问题，单是答案不能重复这里就过不了

所以还是应该先排序，再双指针遍历，参考第11题接雨水的问题，移动左右指针。

第一次提交的想法，首先排序之后元素有序，外层元素之后的元素都不小于它，所以最开始的时候直接指向i+1和len-1，两个边界，直接求和看能否达到target=0-nums[i]，如果和大于target，说明整数，也就是右边界选大了；如果小于target，说明左边界最小值选小了，需要把左指针向右移动，如果刚好等于target，就可以考虑加入答案集合了。

第一次设想的去重是判别ans的最后一个元素和目标要输入的内容是否一致，大体上是对的，但是这个错因在第二次提交后才体现出来，因为最外层可能重复，导致ans的最后元不是首元相同的类别

期间还多想了一个判别条件，就是要求左指针指向的数字必须小于0，保证负数，实际上是完全不需要的，所以第一次提交在case [1,1,-2]上输出的是空结果，报错了。

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums = sorted(nums)
        ans = []
        for i in range(len(nums)):
            if nums[i] > 0:
                break
            target = 0-nums[i]
            # 后面的元素特征：不小于nums[i]且递增
            left = i+1
            right = len(nums) - 1
            while left<right and nums[left]<=0:
                cur = nums[left] + nums[right]
                if cur > target:
                    right -= 1
                elif cur < target:
                    left += 1
                else:
                    if not (len(ans)>0 and nums[left]==ans[-1][1] and nums[right]==ans[-1][2]):
                        ans.append([nums[i],nums[left],nums[right]])
                    right -= 1
                    left += 1
        return ans
```

二次提交，改变的地方在i的终止条件，while循环对左边界的判别修改为与target比较，如果最小值已经大于target了，那就没有必要继续移动指针了。

错误的case：

[-4,-2,-2,-2,0,1,2,2,2,3,3,4,4,6,6]

预期结果：

[[-4,-2,6],[-4,0,4],[-4,1,3],[-4,2,2],[-2,-2,4],[-2,0,2]]

输出结果：

[[-4,-2,6],[-4,0,4],[-4,1,3],[-4,2,2],[-2,-2,4],[-2,0,2],[-2,-2,4],[-2,0,2]]

去重没搞好

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums = sorted(nums)
        ans = []
        for i in range(len(nums)-2):
            if nums[i] > 0:
                break
            target = 0-nums[i]
            # 后面的元素特征：不小于nums[i]且递增
            left = i+1
            right = len(nums) - 1
            while left<right and nums[left]<=target:
                cur = nums[left] + nums[right]
                if cur > target:
                    right -= 1
                elif cur < target:
                    left += 1
                else:
                    if not (len(ans)>0 and nums[left]==ans[-1][1] and nums[right]==ans[-1][2]):
                        ans.append([nums[i],nums[left],nums[right]])
                    right -= 1
                    left += 1
        return ans
```

第三次提交：

主要改动：在外层for循环中添加了对i的跳过判定，如果当前元素和上一个元素一致，直接跳过，因为上一个元素已经判断过了。

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums = sorted(nums)
        ans = []
        for i in range(len(nums)-2):
            if nums[i] > 0:
                break
            if i>0 and nums[i]==nums[i-1]:
                continue
            target = 0-nums[i]
            # 后面的元素特征：不小于nums[i]且递增
            left = i+1
            right = len(nums) - 1
            while left<right and nums[left]<=target:
                cur = nums[left] + nums[right]
                if cur > target:
                    right -= 1
                elif cur < target:
                    left += 1
                else:
                    if not (len(ans)>0 and nums[left]==ans[-1][1] and nums[right]==ans[-1][2]):
                        ans.append([nums[i],nums[left],nums[right]])
                    right -= 1
                    left += 1
        return ans

```

结果：

![image-20260121210040408](./assets/image-20260121210040408.png)

## Python参考答案

为方便双指针以及跳过相同元素，先把 *nums* 排序。

枚举 *nums*[*i*]，问题变成 *nums*[*j*]+*nums*[*k*]=−*nums*[*i*]

在外层循环中，如果发现 nums[i]=nums[i−1]，那么 nums[i] 与后面两个数组成的和为 0 的三元组，nums[i−1] 也能组成一模一样的三元组，这就重复了，所以遇到 nums[i]=nums[i−1] 的情况，直接 continue。

在内层循环中，当三数之和等于 0 时，为避免把相同的三元组计入答案，跳过后续相同的 *nums*[*j*] 和 *nums*[*k*]（也可以只跳过相同的 *nums*[*j*]）。

可以继续优化的点：

**优化一**：如果 nums[i] 与后面最小的两个数相加 nums[i]+nums[i+1]+nums[i+2]>0，那么后面不可能存在三数之和等于 0，break 外层循环。

**优化二**：如果 nums[i] 与后面最大的两个数相加 nums[i]+nums[n−2]+nums[n−1]<0，那么内层循环不可能存在三数之和等于 0，但继续枚举，nums[i] 可以变大，所以后面还有机会找到三数之和等于 0，continue 外层循环。

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums.sort()
        ans = []
        n = len(nums)
        for i in range(n - 2):
            x = nums[i]
            if i > 0 and x == nums[i - 1]:  # 跳过重复数字
                continue
            if x + nums[i + 1] + nums[i + 2] > 0:  # 优化一
                break
            if x + nums[-2] + nums[-1] < 0:  # 优化二
                continue
            j = i + 1
            k = n - 1
            while j < k:
                s = x + nums[j] + nums[k]
                if s > 0:
                    k -= 1
                elif s < 0:
                    j += 1
                else:  # 三数之和为 0
                    ans.append([x, nums[j], nums[k]])
                    j += 1
                    while j < k and nums[j] == nums[j - 1]:  # 跳过重复数字
                        j += 1
                    k -= 1
                    while k > j and nums[k] == nums[k + 1]:  # 跳过重复数字
                        k -= 1
        return ans
```

上面跳过重复数字的循环，也可以这样写：如果 nums[i]+nums[j]+nums[k]=0，并且 j>i+1 且 nums[j]=nums[j−1]，则说明我们在之前找到了一样的三元组。

比如 nums=[−3,1,1,2,2]，首先我们找到 nums[0]+nums[1]+nums[4]=0，在 j 加一，k 减一后，发现 nums[0]+nums[2]+nums[3] 也等于 0，但 nums[2]=nums[1]（这同时说明 nums[3]=nums[4]），是重复的三元组，不能加入答案。

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums.sort()
        ans = []
        n = len(nums)
        for i in range(n - 2):
            x = nums[i]
            if i > 0 and x == nums[i - 1]:  # 跳过重复数字
                continue
            if x + nums[i + 1] + nums[i + 2] > 0:  # 优化一
                break
            if x + nums[-2] + nums[-1] < 0:  # 优化二
                continue
            j = i + 1
            k = n - 1
            while j < k:
                s = x + nums[j] + nums[k]
                if s > 0:
                    k -= 1
                elif s < 0:
                    j += 1
                else:  # 三数之和为 0
                    # j = i+1 表示刚开始双指针，此时 j 左边没有数字
                    # nums[j] != nums[j-1] 说明与上一轮循环的三元组不同
                    if j == i + 1 or nums[j] != nums[j - 1]:
                        ans.append([x, nums[j], nums[k]])
                    j += 1
                    k -= 1
        return ans
```

## Python收获



# 42.接雨水（困难）

## 题目描述

给定 `n` 个非负整数表示每个宽度为 `1` 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。

 

**示例 1：**

![img](https://assets.leetcode.cn/aliyun-lc-upload/uploads/2018/10/22/rainwatertrap.png)

```
输入：height = [0,1,0,2,1,0,1,3,2,1,2,1]
输出：6
解释：上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。 
```

**示例 2：**

```
输入：height = [4,2,0,3,2,5]
输出：9
```

 

**提示：**

- `n == height.length`
- `1 <= n <= 2 * 104`
- `0 <= height[i] <= 105`

## 我的Python解答

第一次提交，没有考虑最高点的情况，导致[4,2,0,3,2,5]返回的结果是4而不是9，后面尝试很多次来对最高点判决，都有对应的错误示例，所以我准备直接看答案了，用时太久了

```python
class Solution:
    def trap(self, height: List[int]) -> int:
        # 本质上是在从递减的起点找递增的终点，直接双指针
        n = len(height)
        ans = 0
        start = 0
        cur_max = height[0] # 当前扫过区域的最大值
        cur_max_pos = 0
        second_max = height[0]
        second_max_pos = 0
        while start<n-1:
            # 找mid最低点
            mid = start
            while mid<n-1 and height[mid]>=height[mid+1]:
                mid += 1
            # 找end最高点
            end = mid+1
            while end<n-1 and height[end]<=height[end+1]:
                end += 1
            if mid == n-1:
                break
            else:
                # 已知区间，求接水面积
                max_h = min(height[start],height[end])
                for i in range(start+1,end):
                    if height[i]<max_h:
                        ans += max_h-height[i]
                if height[end] > cur_max:
                    # 左侧需要补充
                    ans += (cur_max-second_max)*(end-cur_max_pos-1)
                elif height[end] < cur_max:
                    second_max_pos = end
                    second_max = height[end]
                else:
                    if start!=cur_max_pos:
                        ans += (cur_max-second_max)*(end-cur_max_pos-1)
                    cur_max_pos = end
                start = end
        return ans
```

## Pyhton参考答案

方法1:前后缀分离

```python
class Solution:
    def trap(self, height: List[int]) -> int:
        n = len(height)
        pre_max = [0] * n  # pre_max[i] 表示从 height[0] 到 height[i] 的最大值
        pre_max[0] = height[0]
        for i in range(1, n):
            pre_max[i] = max(pre_max[i - 1], height[i])

        suf_max = [0] * n  # suf_max[i] 表示从 height[i] 到 height[n-1] 的最大值
        suf_max[-1] = height[-1]
        for i in range(n - 2, -1, -1):
            suf_max[i] = max(suf_max[i + 1], height[i])

        ans = 0
        for h, pre, suf in zip(height, pre_max, suf_max):
            ans += min(pre, suf) - h  # 累加每个水桶能接多少水
        return ans
```

- 时间复杂度：O(*n*)，其中 *n* 为 *height* 的长度。
- 空间复杂度：O(*n*)。

方法2:相向双指针

```python
class Solution:
    def trap(self, height: List[int]) -> int:
        ans = pre_max = suf_max = 0
        left, right = 0, len(height) - 1
        while left < right:
            pre_max = max(pre_max, height[left])
            suf_max = max(suf_max, height[right])
            if pre_max < suf_max:
                ans += pre_max - height[left]
                left += 1
            else:
                ans += suf_max - height[right]
                right -= 1
        return ans
```

- 时间复杂度：O(*n*)，其中 *n* 为 *height* 的长度。
- 空间复杂度：O(1)，仅用到若干额外变量。

方法3:单调栈

**找上一个更大元素，在找的过程中填坑。**

```python
class Solution:
    def trap(self, height: List[int]) -> int:
        ans = 0
        st = []
        for i, h in enumerate(height):
            while st and height[st[-1]] <= h:
                bottom_h = height[st.pop()]
                if not st:  # 栈是空的
                    break
                left = st[-1]
                dh = min(height[left], h) - bottom_h  # 面积的高
                ans += dh * (i - left - 1)
            st.append(i)
        return ans
```

时间复杂度：O(n)，其中 n 为 height 的长度。虽然我们写了个二重循环，但站在每个元素的视角看，这个元素在二重循环中最多入栈出栈各一次，因此循环次数之和是 O(n)，所以时间复杂度是 O(n)。
空间复杂度：O(min(n,U))，其中 U=max(height)−min(height)+1。注意栈中没有重复元素，在 height 值域很小的情况下，空间复杂度主要取决于 height 的值域范围。
