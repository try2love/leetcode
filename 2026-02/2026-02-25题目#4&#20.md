# 4.寻找两个正序数组的中位数（困难）

## 题目描述

给定两个大小分别为 `m` 和 `n` 的正序（从小到大）数组 `nums1` 和 `nums2`。请你找出并返回这两个正序数组的 **中位数** 。

算法的时间复杂度应该为 `O(log (m+n))` 。

 

**示例 1：**

```
输入：nums1 = [1,3], nums2 = [2]
输出：2.00000
解释：合并数组 = [1,2,3] ，中位数 2
```

**示例 2：**

```
输入：nums1 = [1,2], nums2 = [3,4]
输出：2.50000
解释：合并数组 = [1,2,3,4] ，中位数 (2 + 3) / 2 = 2.5
```

**提示：**

- `nums1.length == m`
- `nums2.length == n`
- `0 <= m <= 1000`
- `0 <= n <= 1000`
- `1 <= m + n <= 2000`
- `-106 <= nums1[i], nums2[i] <= 106`

## 我的Python解答

```python
class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:
        if len(nums1) < len(nums2):
            nums1, nums2 = nums2, nums1
        m, n = len(nums1), len(nums2) # m>=n
        if m+n==3:
            return nums1[0] if nums1[0]>=nums2[0] else nums2[0]
        flag = (m+n)%2 # 1表示奇数，直接返回中间值
        k = (m+n-1)//2
        if nums1[0] >= nums2[-1]:
            new_num = nums2+nums1
            return new_num[k] if flag else (new_num[k]+new_num[k+1])/2
        if nums1[-1] <= nums2[0]:
            new_num = nums1 + nums2
            return new_num[k] if flag else (new_num[k]+new_num[k+1])/2
        # 想象有一根长为k的绳子，最开始绳子尽可能占用长数组nums1
        # i，j分别对应两个数组
        # 什么时候结束？ nums1[i-1] < nums2[j] 且 nums2[j-1] < nums1[i]
        # 绳子从nums1的0号到k-1号，简单推理可以得知长序列一定有绳子
        left, right = 0, min(k,m) # 每次只用管一个序列，因为另一个序列可以推理
        while left<right:
            mid = (left+right)//2
            j = k+m-mid
            if (1<=mid<m and 1<=j<n and nums1[mid-1]<nums2[j] and nums2[j-1] < nums1[mid]) or mid==0:
                return nums1[mid] if flag else (nums1[mid]+nums2[j])/2
            elif nums1[mid-1]>=nums2[j]:
                right = mid
            left = mid+1
```

我没招数了，只知道模拟一个长度为k的绳子，k是左右区间闭合

## Python参考答案

```python
class Solution:
    def findMedianSortedArrays(self, a: List[int], b: List[int]) -> float:
        if len(a) > len(b):
            a, b = b, a

        m, n = len(a), len(b)
        # 循环不变量：a[left] <= b[j+1]
        # 循环不变量：a[right] > b[j+1]
        left, right = -1, m
        while left + 1 < right:  # 开区间 (left, right) 不为空
            i = (left + right) // 2
            j = (m + n - 3) // 2 - i
            if a[i] <= b[j + 1]:
                left = i  # 缩小二分区间为 (i, right)
            else:
                right = i  # 缩小二分区间为 (left, i)

        # 此时 left 等于 right-1
        # a[left] <= b[j+1] 且 a[right] > b[j'+1] = b[j]，所以答案是 i=left
        i = left
        j = (m + n - 3) // 2 - i
        ai = a[i] if i >= 0 else -inf
        bj = b[j] if j >= 0 else -inf
        ai1 = a[i + 1] if i + 1 < m else inf
        bj1 = b[j + 1] if j + 1 < n else inf
        max1 = max(ai, bj)
        min2 = min(ai1, bj1)
        return max1 if (m + n) % 2 else (max1 + min2) / 2
```

# 20.有效的括号

## 题目描述

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。

有效字符串需满足：

1. 左括号必须用相同类型的右括号闭合。
2. 左括号必须以正确的顺序闭合。
3. 每个右括号都有一个对应的相同类型的左括号。

**示例 1：**

**输入：**s = "()"

**输出：**true

**示例 2：**

**输入：**s = "()[]{}"

**输出：**true

**示例 3：**

**输入：**s = "(]"

**输出：**false

**示例 4：**

**输入：**s = "([])"

**输出：**true

**示例 5：**

**输入：**s = "([)]"

**输出：**false

**提示：**

- `1 <= s.length <= 104`
- `s` 仅由括号 `'()[]{}'` 组成

## 我的解答

总感觉写的复杂了

```python
class Solution:
    left_scale = ["(", "[", "{"]
    right_scale = [")", "]", "}"]
    def isValid(self, s: str) -> bool:
        # 维护一个栈，左括号入栈，右括号则不断出战，直到出来对应左括号
        q = deque()
        flag = False
        for element in s:
            if element in ["(", "[", "{"]:
                q.append(element)
            else:
                if q:
                    idx = self.right_scale.index(element)
                    left = q[-1]
                    if left != self.left_scale[idx]:
                        return False
                    else:
                        q.pop()
                else:
                    return False
        return len(q)==0
```

结果：

![image-20260226000501762](./assets/image-20260226000501762.png)

## 参考答案

```python
class Solution:
    def isValid(self, s: str) -> bool:
        if len(s) % 2:  # s 长度必须是偶数
            return False
        mp = {')': '(', ']': '[', '}': '{'}
        st = []
        for c in s:
            if c not in mp:  # c 是左括号
                st.append(c)  # 入栈
            elif not st or st.pop() != mp[c]:  # c 是右括号
                return False  # 没有左括号，或者左括号类型不对
        return not st  # 所有左括号必须匹配完毕

class Solution:
    def isValid(self, s: str) -> bool:
        if len(s) % 2:  # s 长度必须是偶数
            return False
        mp = {'(': ')', '[': ']', '{': '}'}
        st = []
        for c in s:
            if c in mp:  # c 是左括号
                st.append(mp[c])  # 入栈对应的右括号
            elif not st or st.pop() != c:  # c 是右括号
                return False  # 没有左括号，或者左括号类型不对
        return not st  # 所有左括号必须匹配完毕
    
class Solution:
    def isValid(self, s: str) -> bool:
        if len(s) % 2:  # s 长度必须是偶数
            return False
        st = []
        for c in s:
            if c == '(':
                st.append(')')  # 入栈对应的右括号
            elif c == '[':
                st.append(']')
            elif c == '{':
                st.append('}')
            elif not st or st.pop() != c:  # c 是右括号
                return False  # 没有左括号，或者左括号类型不对
        return not st  # 所有左括号必须匹配完毕
```

