# 题目单

## 0）开场高频：项目与工程能力（几乎每轮都会问）

1. 你负责过的系统/服务是做什么的？**QPS、延迟、SLA、数据量**是多少？
2. 你做过的一个“最难 bug/事故”是什么？怎么定位？怎么复盘？
3. 你做过的“性能优化”是什么？瓶颈怎么找到的？收益是多少？
4. 你如何保证上线质量？（灰度、回滚、监控、告警、压测、回归）
5. 你如何写出可维护代码？（接口设计、模块边界、单测、日志）

追问套路：面试官会强行把你回答落到**数字**上：P99、QPS、内存、CPU、连接数、磁盘IO、缓存命中率。

------

## 1）C++语言与工程基础（C++后台核心）

### 1.1 内存管理与对象模型（必问）

1. 栈 vs 堆：生命周期、性能、碎片、缓存友好性
2. new/delete vs malloc/free 区别；placement new 用途
3. 拷贝构造/拷贝赋值/移动构造/移动赋值：**Rule of 3/5/0**
4. RAII：为什么能减少泄漏？典型封装（锁、文件句柄、socket）
5. 智能指针：unique_ptr/shared_ptr/weak_ptr 的使用场景与坑
   - shared_ptr 的引用计数在哪？线程安全到什么程度？
   - weak_ptr 解决什么问题？如何避免循环引用？
6. 内存泄漏怎么定位？（ASan、valgrind、tcmalloc profiling）
7. 对齐与padding：struct内存布局、cache line、false sharing

追问：

- shared_ptr 能否跨线程安全吗？“对象本身线程安全≠引用计数线程安全”怎么解释？
- move 语义什么时候真的省？什么时候反而更慢？

### 1.2 语言特性与性能（高频）

1. virtual/多态：虚表结构、虚函数调用开销、析构函数为什么常为 virtual
2. inline、模板与编译期：ODR、头文件膨胀、编译慢如何治理
3. const/constexpr：用途区别
4. 引用 vs 指针；左值/右值；完美转发、std::forward
5. 异常 vs 错误码：服务端为何常用 error code？异常的成本
6. STL：vector/list/map/unordered_map 的复杂度、迭代器失效规则
7. unordered_map 的 rehash 触发与性能抖动；reserve 的工程意义

追问：

- vector 扩容为什么是指数？什么时候会触发拷贝/移动？
- unordered_map 哈希冲突最坏情况？如何防（自定义hash/rehash策略）？

### 1.3 编译/链接/构建（容易被忽视但会问）

1. 编译流程：预处理→编译→汇编→链接
2. 静态库 vs 动态库：加载时机、符号解析、版本兼容
3. CMake 常见写法：target_include_directories/target_link_libraries
4. Debug/Release 差异；O2/O3、LTO、-g、符号表

------

## 2）操作系统与Linux（后台必考）

### 2.1 进程与线程（必问）

1. 进程 vs 线程区别；上下文切换成本
2. 线程模型：用户态线程 vs 内核线程（了解）
3. fork/exec：copy-on-write
4. 线程同步：mutex/condvar/semaphore/spinlock，分别适用什么场景
5. 读写锁什么时候更快？什么时候更慢？
6. 线程池设计：任务队列、work stealing、饥饿与优先级
7. 死锁四条件、如何排查（锁顺序、try_lock、超时）

追问：

- 什么时候用自旋锁？为什么自旋锁在单核会灾难？
- condition_variable 为什么要 while 而不是 if？

### 2.2 内存与性能（高频）

1. 虚拟内存、页表、TLB
2. mmap 的用途：零拷贝、文件映射
3. 缓存：page cache、buffer cache
4. NUMA：多路CPU下内存亲和性
5. OOM 发生时如何排查？（dmesg、oom killer日志）

### 2.3 IO模型（网络服务核心）

1. 阻塞/非阻塞 IO
2. IO多路复用：select/poll/epoll 区别与复杂度
3. LT/ET 模式区别，ET 常见坑（必须读到 EAGAIN）
4. 同步IO vs 异步IO（Linux AIO/io_uring 了解即可）
5. 零拷贝：sendfile/splice、mmap、DMA 直觉

追问：

- epoll 为什么更适合高并发？
- ET 比 LT 强在哪？为什么更难写对？

------

## 3）计算机网络（后台必考）

### 3.1 TCP/IP（必问）

1. TCP 三次握手/四次挥手；TIME_WAIT 为什么存在？
2. 半连接队列、全连接队列，SYN flood 防护
3. TCP 粘包/拆包原因与解决（长度前缀/分隔符）
4. Nagle 算法、延迟确认 ACK，何时关闭 TCP_NODELAY
5. 流量控制 vs 拥塞控制：窗口、慢启动、拥塞避免、快重传
6. KeepAlive 与应用层心跳区别
7. socket 选项：SO_REUSEADDR/SO_REUSEPORT、backlog

追问：

- 大量 TIME_WAIT 怎么办？（短连接、连接复用、参数调优、负载均衡）
- 连接突然断开你如何定位（RST/FIN/超时）？

### 3.2 HTTP/HTTPS（高频）

1. HTTP1.1 keep-alive、pipeline（了解）
2. HTTP2 多路复用、头部压缩、队头阻塞差异
3. HTTPS：TLS握手大致流程、证书、对性能影响
4. 常见状态码、幂等方法（GET/PUT/POST/DELETE）

### 3.3 RPC与服务治理（很多后台都会问）

1. gRPC/Thrift/自研RPC：IDL、序列化、连接管理
2. 负载均衡：随机/轮询/一致性哈希/最小连接数
3. 服务发现：注册中心、健康检查
4. 超时、重试、幂等、熔断、限流：组合拳怎么设计
5. Trace：链路追踪（trace_id/span）

追问：

- 重试会放大流量，怎么避免雪崩？（指数退避、熔断、限流）
- 幂等怎么做？（请求ID、去重表、业务唯一键）

------

## 4）并发编程与锁（C++后台高频重灾区）

1. 竞态条件怎么出现？举例（读写共享变量）
2. 原子操作：CAS、ABA问题；std::atomic 内存序（知道 acquire/release）
3. 无锁队列/环形队列：为什么快？难点是什么？
4. false sharing 是什么？如何避免？（padding、alignas、分离热字段）
5. 并发下的性能分析：锁争用怎么看（perf、火焰图）

追问：

- 你如何证明线程安全？怎么做压力测试？
- 什么时候“锁更少”反而更慢（忙等、cache抖动、伪共享）？

------

## 5）数据库与缓存（后台必问）

### 5.1 MySQL（高频）

1. 索引原理：B+树；联合索引最左前缀
2. 覆盖索引、回表、索引下推
3. explain 怎么看；type、rows、extra
4. 事务：ACID；隔离级别；幻读/不可重复读
5. MVCC、undo log、redo log；binlog
6. 锁：行锁/间隙锁/next-key lock；死锁排查
7. 慢查询怎么治理：索引、SQL改写、分库分表（了解）

追问：

- 为什么 B+树适合范围查询？
- 为什么 where 条件写法会导致索引失效？

### 5.2 Redis（必问）

1. 常用数据结构：string/hash/list/set/zset 对应场景
2. 过期策略、内存淘汰策略（LRU/LFU等）
3. 持久化：RDB/AOF
4. 主从复制、哨兵、集群；一致性与延迟
5. 缓存穿透/击穿/雪崩：分别是什么？怎么解决？
6. 分布式锁：SET NX PX；续租；RedLock争议（知道有争议即可）

追问：

- 缓存一致性：先写库还是先删缓存？为什么有坑？怎么补（双删、延迟删、消息队列）
- 热点 key 怎么办？（本地缓存、分片、限流）

------

## 6）系统设计题（面试官最爱）

### 6.1 基础系统设计（实习也会问）

1. 设计一个短链接服务：写入、读扩散、缓存、过期、冲突
2. 设计一个消息队列/任务队列：至少一次/至多一次/恰好一次语义
3. 设计一个限流系统：令牌桶/漏桶；分布式限流
4. 设计一个排行榜：zset、分片、topK、更新频率
5. 设计一个文件上传服务：分片上传、断点续传、校验、幂等

追问套路：

- 数据量上来怎么办（分片、缓存、异步）
- 一致性如何保证（事务、幂等、补偿）
- 宕机怎么恢复（持久化、重放、主从）

### 6.2 高并发架构思维（更偏中高阶）

1. 为什么要做异步化？怎么用 MQ 解耦？
2. 如何做降级与熔断？（避免级联故障）
3. 如何做灰度发布？如何回滚？
4. 如何做容量规划？（QPS、连接数、CPU核数、网络带宽）

------

## 7）工程化：日志、监控、排障（非常加分）

1. 线上日志怎么打？（结构化日志、trace_id、采样）
2. 监控指标：QPS、错误率、P50/P95/P99、CPU、内存、GC（若有）、连接数
3. 告警如何避免噪声（去抖、分级）
4. 线上 CPU 飙升怎么查（top、perf、火焰图）
5. 内存泄漏/增长怎么查（heap profile、ASan、valgrind）
6. 网络问题怎么查（ss/netstat、tcpdump、wireshark）
7. core dump 如何分析（gdb、bt、符号）

------

## 8）安全与稳定性（后台也常问）

1. 常见漏洞：SQL注入、XSS（如果有前端）、CSRF、命令注入
2. 鉴权：JWT/Session、过期刷新、权限模型（RBAC）
3. 防刷：限流、验证码、风控（了解）
4. 数据加密：传输TLS、存储加密（概念即可）

------

## 9）手撕代码/算法（后台岗仍会考）

常见考法是“工程+算法混合”：

1. LRU / LFU（必背）
2. 线程安全的单例（double-checked locking 的坑）
3. 生产者-消费者（condvar）
4. 读写锁的 cache（并发 map）
5. topK、滑动窗口、字符串解析（协议解析）

追问：

- 为什么要用 while wait？
- iterator 失效怎么避免？

------

## 10）建议你现在优先背的“Top 40 高频题”（最短路径）

如果你时间紧，就先把下面 40 个练到能讲清（每题 60 秒版）：

1. TCP 三次握手/四次挥手、TIME_WAIT
2. 粘包拆包解决
3. epoll LT/ET + 常见坑
4. 线程 vs 进程
5. 死锁四条件与排查
6. mutex/condvar 使用与 while wait
7. 线程池设计要点
8. shared_ptr/weak_ptr 循环引用
9. vector 扩容与迭代器失效
10. unordered_map rehash 抖动与 reserve
11. RAII 为什么重要
12. 内存泄漏怎么查（工具）
13. MySQL B+树、联合索引最左前缀
14. 覆盖索引/回表
15. MVCC + 隔离级别
16. Redis 数据结构场景
17. 缓存穿透/击穿/雪崩
18. 缓存一致性策略
19. 分布式锁实现要点与坑
20. RPC 超时/重试/幂等/熔断
21. 限流：令牌桶 vs 漏桶
22. 服务发现与负载均衡策略
23. 线上 P99 延迟升高如何排查
24. CPU 飙升如何排查（perf/火焰图）
25. 内存增长如何排查
26. tcpdump 能看出什么
27. 设计一个短链接服务
28. 设计一个任务队列（至少一次语义）
29. 设计一个排行榜
30. 设计一个上传服务（断点续传）
31. 如何做灰度/回滚
32. 如何做日志与trace
33. LRU 实现
34. 生产者消费者
35. CAS/ABA（了解即可）
36. false sharing
37. mmap / sendfile（了解即可）
38. Nagle 与 TCP_NODELAY
39. KeepAlive vs 应用心跳
40. 常见安全：SQL注入、鉴权

# 2026-04-02题目单

下面我按“**腾讯后台开发高频八股**”来给你整理，尽量只放**最可能被问到**的，不把题库铺得太散。
我会按 **高频核心题** 和 **次高频补充题** 来分，你优先准备前者。

------

## 1. C++

### 高频核心题

1. `new` 和 `malloc` 的区别是什么？
2. `delete` 和 `free` 的区别是什么？
3. `new/delete` 底层做了什么？
4. C++ 的三大特性是什么？分别怎么体现？
5. 重载、重写、隐藏的区别是什么？
6. `const` 的作用有哪些？`const` 修饰指针有哪几种情况？
7. 引用和指针的区别是什么？
8. 左值、右值、右值引用是什么？
9. `std::move` 一定会发生移动吗？
10. 深拷贝和浅拷贝的区别是什么？
11. 拷贝构造、拷贝赋值、移动构造、移动赋值分别在什么场景下触发？
12. 为什么需要移动语义？
13. 什么是 RAII？有什么好处？
14. 智能指针有哪些？`shared_ptr`、`unique_ptr`、`weak_ptr` 的区别是什么？
15. `shared_ptr` 的引用计数原理是什么？线程安全吗？
16. 为什么 `shared_ptr` 会有循环引用？怎么解决？
17. 虚函数的作用是什么？
18. 虚函数表是怎么实现多态的？
19. 构造函数可以是虚函数吗？析构函数为什么常常要写成虚函数？
20. `static` 的作用有哪些？
21. `inline` 的作用是什么？一定会内联吗？
22. `vector` 的底层实现是什么？扩容机制是什么？
23. `vector` 和 `list` 的区别是什么？
24. `map` 和 `unordered_map` 的区别是什么？
25. `unordered_map` 为什么快？它的底层是什么？
26. 哈希冲突怎么解决？
27. `set` / `map` 为什么一般是红黑树而不是 AVL？
28. `auto` 和 `decltype` 的区别是什么？
29. 模板是什么？模板和宏有什么区别？
30. 什么是函数模板和类模板？
31. `nullptr` 和 `NULL` 的区别是什么？
32. C++11 你用过哪些新特性？
33. `volatile` 是干什么的？它能保证线程安全吗？
34. `sizeof` 和 `strlen` 的区别是什么？
35. 异常处理机制 `try-catch-throw` 的底层原理了解吗？
36. `mutex`、`lock_guard`、`unique_lock` 的区别是什么？
37. 死锁产生的条件是什么？怎么避免？
38. 线程同步有哪些方式？
39. 进程和线程在 C++ 开发里分别怎么用？
40. 你项目里最常用的 STL 容器是什么？为什么？

### 次高频补充题

1. 红黑树的性质是什么？
2. `emplace_back` 和 `push_back` 的区别是什么？
3. `reserve` 和 `resize` 的区别是什么？
4. `vector` 迭代器失效场景有哪些？
5. `map` / `unordered_map` 的迭代器失效情况？
6. `static_cast`、`dynamic_cast`、`const_cast`、`reinterpret_cast` 的区别是什么？
7. lambda 表达式底层是什么？
8. `std::function` 和函数指针的区别？
9. 条件变量 `condition_variable` 的使用场景是什么？
10. 原子变量 `atomic` 和锁的区别是什么？
11. 内存对齐是什么？为什么要内存对齐？
12. 什么是野指针？什么是悬空指针？
13. 构造函数初始化列表为什么更推荐？
14. 单例模式怎么写？线程安全怎么保证？
15. 工厂模式、观察者模式、生产者消费者模型了解吗？

------

## 2. Linux

### 高频核心题

1. Linux 下查看进程常用哪些命令？
2. `ps -ef` 和 `ps aux` 有什么区别？
3. `top` 你怎么看 CPU 和内存占用？
4. 怎么查某个端口被谁占用了？
5. `netstat`、`ss`、`lsof` 分别怎么用？
6. 如何杀掉一个进程？
7. `kill` 和 `kill -9` 的区别是什么？
8. Linux 下如何查看日志？
9. `tail -f`、`grep`、`awk`、`sed` 常见用法是什么？
10. 如何查找文件？
11. `find` 和 `grep` 的区别是什么？
12. 如何查看磁盘使用情况？
13. `df` 和 `du` 的区别是什么？
14. 如何查看内存使用情况？
15. `free -h` 里各字段是什么意思？
16. Linux 文件权限 `rwx` 分别是什么意思？
17. `chmod 755` 表示什么？
18. 硬链接和软链接的区别是什么？
19. inode 是什么？
20. Linux 下一个文件删除了，磁盘空间为什么可能没有释放？
21. 什么是僵尸进程？什么是孤儿进程？
22. 怎么查看 CPU 负载？load average 是什么？
23. 用户态和内核态的区别是什么？
24. Linux 下进程间通信方式有哪些？
25. 管道和消息队列、共享内存的区别是什么？
26. `select`、`poll`、`epoll` 的区别是什么？
27. `epoll` 为什么高效？
28. 阻塞 IO、非阻塞 IO、IO 多路复用分别是什么？
29. Linux 下怎么排查线上服务 CPU 飙高？
30. Linux 下怎么排查内存泄漏或内存暴涨？

### 次高频补充题

1. Shell 脚本基础会问到哪些？
2. `nohup`、`&`、`screen`、`tmux` 的区别？
3. 环境变量怎么查看和设置？
4. `export` 是做什么的？
5. Linux 启动一个程序的流程大致是什么？
6. 动态链接库和静态链接库的区别是什么？
7. `ldd` 是干什么的？
8. `strace` 和 `gdb` 用过吗？
9. 页缓存 page cache 是什么？
10. 零拷贝在 Linux 里怎么实现？

------

## 3. Docker

### 高频核心题

1. Docker 是什么？解决了什么问题？
2. Docker 和虚拟机的区别是什么？
3. 镜像和容器的区别是什么？
4. Docker 的底层原理了解吗？
5. Docker 为什么比虚拟机轻量？
6. `Dockerfile` 常见指令有哪些？
7. `CMD` 和 `ENTRYPOINT` 的区别是什么？
8. `COPY` 和 `ADD` 的区别是什么？
9. Docker 镜像是怎么分层的？
10. 分层存储有什么好处？
11. 容器的数据如何持久化？
12. 数据卷 volume 是什么？
13. 端口映射是怎么做的？
14. 容器之间如何通信？
15. Docker 网络模式有哪些？
16. `bridge`、`host`、`none` 模式的区别是什么？
17. Docker 启动慢或镜像很大，怎么优化？
18. 多阶段构建是什么？
19. 如何进入一个正在运行的容器？
20. 如何查看容器日志？
21. Docker 容器退出了怎么排查？
22. 容器和宿主机文件共享怎么做？
23. Docker Compose 是什么？适合什么场景？
24. 容器重启策略有哪些？
25. Docker 常见安全问题有哪些？

### 次高频补充题

1. namespace 和 cgroup 分别是什么？
2. Docker 如何限制 CPU 和内存资源？
3. 镜像仓库私有化了解吗？
4. Docker 和 Kubernetes 的关系是什么？
5. 容器编排为什么需要 K8s？
6. 容器内一个进程挂了会怎么样？
7. 为什么说一个容器最好只跑一个主进程？

------

## 4. 操作系统

### 高频核心题

1. 进程和线程的区别是什么？
2. 线程切换和进程切换哪个开销更大？为什么？
3. 进程间通信有哪些方式？
4. 线程同步有哪些方式？
5. 什么是互斥？什么是同步？
6. 什么是死锁？死锁的四个必要条件是什么？
7. 如何避免死锁？
8. 银行家算法了解吗？
9. 什么是上下文切换？
10. 用户态和内核态的区别是什么？
11. 什么情况下会发生系统调用？
12. 中断和异常的区别是什么？
13. 虚拟内存是什么？为什么需要虚拟内存？
14. 分页和分段的区别是什么？
15. 页表是干什么的？
16. 快表 TLB 是什么？
17. 缺页中断是什么？
18. 页面置换算法有哪些？LRU 怎么实现？
19. 堆和栈的区别是什么？
20. 内存泄漏和内存溢出的区别是什么？
21. 什么是缓冲区溢出？
22. 什么是零拷贝？
23. DMA 是什么？
24. 什么是文件描述符？
25. 从输入 URL 到页面显示，中间涉及哪些 OS 层面的内容？
26. 什么是系统调用？常见系统调用有哪些？
27. 阻塞和非阻塞的区别是什么？
28. 同步和异步的区别是什么？
29. select、poll、epoll 本质上属于什么模型？
30. 为什么高并发服务端常用 epoll？

### 次高频补充题

1. 什么是惊群问题？
2. 写时拷贝 COW 是什么？
3. fork 之后父子进程共享什么，不共享什么？
4. mmap 和 read/write 的区别是什么？
5. 文件系统了解多少？
6. ext4 的特点是什么？
7. 从磁盘读取一个文件到用户态，发生了什么？
8. CPU 调度算法有哪些？

------

## 5. 计算机网络

### 高频核心题

1. OSI 七层模型和 TCP/IP 四层模型分别是什么？
2. 应用层、传输层、网络层分别有哪些常见协议？
3. TCP 和 UDP 的区别是什么？
4. TCP 为什么可靠？
5. TCP 头部有哪些重要字段？
6. TCP 三次握手的过程是什么？
7. 为什么是三次握手，不是两次？
8. TCP 四次挥手的过程是什么？
9. 为什么挥手要四次？
10. TIME_WAIT 是什么？为什么需要 TIME_WAIT？
11. 大量 TIME_WAIT 会有什么影响？怎么处理？
12. TCP 的流量控制和拥塞控制分别是什么？
13. 滑动窗口是什么？
14. 拥塞控制有哪些算法？
15. 慢启动和拥塞避免是什么？
16. TCP 粘包/拆包是什么？怎么解决？
17. HTTP 和 HTTPS 的区别是什么？
18. HTTPS 建立连接的过程是什么？
19. TLS 握手大致过程是什么？
20. 对称加密和非对称加密分别用在 HTTPS 哪一步？
21. HTTP1.0、HTTP1.1、HTTP2、HTTP3 的区别是什么？
22. HTTP 常见状态码有哪些？
23. 302 和 301 的区别是什么？
24. 403 和 404 的区别是什么？
25. GET 和 POST 的区别是什么？
26. Cookie、Session、Token 的区别是什么？
27. DNS 查询过程是什么？
28. 输入一个网址到页面显示，中间发生了什么？
29. 什么是长连接和短连接？
30. 什么是 Keep-Alive？
31. WebSocket 和 HTTP 的区别是什么？
32. IP 地址和 MAC 地址的区别是什么？
33. ARP 协议是做什么的？
34. 路由器和交换机的区别是什么？
35. NAT 是什么？
36. 什么是负载均衡？常见策略有哪些？
37. 什么是 CDN？为什么能加速？
38. 什么是反向代理？
39. 什么是 socket？
40. 服务端高并发网络模型常见有哪些？

### 次高频补充题

1. SYN Flood 是什么？怎么防？
2. 半连接队列和全连接队列是什么？
3. backlog 参数有什么作用？
4. 为什么服务端要做心跳机制？
5. 断线重连一般怎么设计？
6. HTTP 幂等性是什么？
7. 重试机制为什么会带来重复请求问题？
8. 如何设计接口保证幂等？
9. 代理和网关的区别是什么？

------

## 腾讯后台开发还特别容易追问的方向

腾讯后台不一定只停留在“背定义”，很喜欢继续追问“**落地场景**”，所以你要额外准备这几类：

### 1. Linux 命令排障题

例如：

- 线上 CPU 100% 你怎么查？
- 线上内存持续上涨你怎么查？
- 某个端口连不上你怎么查？
- 服务日志太大你怎么快速定位错误？

### 2. 网络结合项目题

例如：

- TCP 为什么可靠，具体靠哪些机制？
- 你的服务为什么要用 TCP，不用 UDP？
- HTTP 请求慢可能卡在哪几层？
- 长连接怎么保活？

### 3. C++ 结合工程题

例如：

- 你项目里为什么选 `unordered_map`？
- 为什么这里用智能指针，不用裸指针？
- 你怎么避免内存泄漏？
- 多线程访问共享资源时怎么保证安全？

### 4. Docker 结合部署题

例如：

- 你怎么把服务打包成镜像？
- 容器挂了怎么排查？
- 为什么镜像会很大，怎么优化？
- 容器日志怎么查看？

## C++

### 1. `new` 和 `malloc` 的区别是什么？

`new` 是 C++ 运算符，`malloc` 是 C 语言库函数。
`new` 在分配内存后会**调用构造函数**完成对象初始化，而 `malloc` 只负责按字节申请一块原始内存，不会初始化对象。
`new` 返回的是对应类型的指针，不需要强转；`malloc` 返回 `void*`，通常需要强转。
失败时，`new` 默认抛出 `bad_alloc` 异常，`malloc` 返回 `NULL`。
对应释放时，`new` 要搭配 `delete`，`malloc` 要搭配 `free`，不能混用。

------

### 2. `delete` 和 `free` 的区别是什么？

`delete` 是 C++ 运算符，`free` 是 C 库函数。
`delete` 在释放内存前会**先调用析构函数**，然后再归还内存；`free` 只释放内存，不会调用析构函数。
所以对于类对象，尤其是内部有资源管理逻辑的对象，必须用 `delete`，不能用 `free`。
同理，`new[]` 必须配 `delete[]`，否则可能导致析构次数不对。

------

### 3. `new/delete` 底层做了什么？

`new` 底层通常分两步。
第一步，调用 `operator new` 申请足够大小的原始内存；第二步，在这块内存上调用构造函数，完成对象初始化。
`delete` 也分两步。
第一步，调用析构函数释放对象持有的资源；第二步，再调用 `operator delete` 归还原始内存。
所以面试里常说：`new = 分配内存 + 构造`，`delete = 析构 + 释放内存`。

------

### 4. C++ 的三大特性是什么？分别怎么体现？

C++ 三大特性是**封装、继承、多态**。
封装是把数据和操作数据的方法放到类里，并通过访问控制符如 `public`、`private` 隐藏内部实现。
继承是子类复用父类的成员和接口，提升代码复用性。
多态主要是运行时多态，依赖虚函数，通过父类指针或引用调用子类重写后的函数，实现“同一接口，不同表现”。

------

### 5. 重载、重写、隐藏的区别是什么？

**重载**发生在同一作用域，函数名相同但参数列表不同，和返回值无关，是编译期多态。
**重写**发生在继承体系中，子类重写父类的虚函数，函数签名基本一致，用于运行时多态。
**隐藏**也发生在继承体系中，子类定义了和父类同名的函数，即使参数不同，也会把父类同名函数隐藏掉。
一句话记：同类不同参叫重载，父子虚函数叫重写，父子同名但不构成重写时往往叫隐藏。

------

### 6. `const` 的作用有哪些？`const` 修饰指针有哪几种情况？

`const` 表示只读，用于提升代码安全性和语义表达。
它可以修饰变量、成员函数、参数、返回值、指针等。
修饰指针时常见三种：

- `const int *p`：指向的内容不能改，指针本身能改。
- `int * const p`：指针本身不能改，指向内容能改。
- `const int * const p`：指针和内容都不能改。

成员函数后面加 `const`，表示这个函数不能修改对象的非 `mutable` 成员。

------

### 7. 引用和指针的区别是什么？

引用本质上是变量的别名，定义时必须初始化，之后不能再绑定到别的对象；指针是一个独立变量，存的是地址，可以改指向。
引用使用起来更像原变量，不需要解引用，语法更自然；指针需要 `*` 取值。
引用通常不能为空，而指针可以为 `nullptr`。
从底层实现看，引用很多编译器也可能通过指针实现，但在语言层面两者语义不同。

------

### 8. 左值、右值、右值引用是什么？

左值通常表示**有名字、可取地址、生命周期较稳定**的对象。
右值通常表示临时对象、字面量或即将销毁的值。
右值引用是 `T&&`，专门用来绑定右值，从而支持**移动语义**和完美转发。
它的意义在于：对临时对象内部资源可以“偷走”而不是拷贝，减少不必要的深拷贝开销。

------

### 9. `std::move` 一定会发生移动吗？

不一定。
`std::move` 本质上只是一个**类型转换**，把一个对象强制转成右值引用，表示“这个对象可以被移动”。
至于最终是否真的发生移动，要看这个类型有没有实现移动构造或移动赋值。
如果没有移动语义，编译器可能还是走拷贝构造。
所以 `std::move` 只是“允许移动”，不是“保证移动”。

------

### 10. 深拷贝和浅拷贝的区别是什么？

浅拷贝只是把对象中的成员值按位复制一份。
如果成员里有指针，浅拷贝后两个对象可能指向同一块堆内存，就容易出现重复释放、悬空指针等问题。
深拷贝会额外重新申请资源，把底层数据也复制一份，使两个对象彼此独立。
所以类里只要涉及动态资源，通常就要自己实现拷贝构造和拷贝赋值，避免默认浅拷贝带来的问题。

------

### 11. 拷贝构造、拷贝赋值、移动构造、移动赋值分别在什么场景下触发？

**拷贝构造**：用一个已存在对象去初始化新对象时触发，比如 `A b = a;`。
**拷贝赋值**：两个对象都已存在，把一个对象赋值给另一个时触发，比如 `b = a;`。
**移动构造**：用右值或将亡值初始化新对象时触发，比如 `A b = std::move(a);`。
**移动赋值**：已存在对象接收右值时触发，比如 `b = std::move(a);`。
核心区别是：构造针对“新对象初始化”，赋值针对“已有对象覆盖”。

------

### 12. 为什么需要移动语义？

因为很多对象内部持有堆内存、文件句柄、缓冲区等资源，深拷贝代价很高。
移动语义允许把这些资源的所有权直接转移给新对象，而不是重新开辟再复制一遍。
这样可以显著降低临时对象传递、返回值传递、容器扩容时的性能开销。
C++11 引入移动语义，本质上就是为了减少无意义拷贝，提高资源管理效率。

------

### 13. 什么是 RAII？有什么好处？

RAII 是“资源获取即初始化”。
核心思想是：把资源的申请和对象生命周期绑定，在构造函数中获取资源，在析构函数中释放资源。
这样对象离开作用域时资源会自动释放，不需要手动回收。
好处是能避免资源泄漏，并且异常安全更好。
典型例子就是智能指针、锁对象、文件流对象。

------

### 14. 智能指针有哪些？`shared_ptr`、`unique_ptr`、`weak_ptr` 的区别是什么？

常见智能指针有 `unique_ptr`、`shared_ptr`、`weak_ptr`。
`unique_ptr` 表示独占所有权，同一时刻只能有一个指针拥有资源，不能拷贝，只能移动。
`shared_ptr` 表示共享所有权，内部有引用计数，多个指针可以共同管理同一资源。
`weak_ptr` 是对 `shared_ptr` 管理对象的弱引用，不增加引用计数，主要用于解决循环引用和观察资源是否还存活。
面试里通常会补一句：默认优先用 `unique_ptr`，只有确实需要共享所有权时再用 `shared_ptr`。

------

### 15. `shared_ptr` 的引用计数原理是什么？线程安全吗？

`shared_ptr` 内部通常维护一个**控制块**，里面包含引用计数、弱引用计数、删除器等信息。
每拷贝一个 `shared_ptr`，强引用计数加一；每析构一个，计数减一；当强引用计数减到 0 时，释放对象本身；当弱引用计数也减到 0 时，再释放控制块。
引用计数的增减通常是原子操作，所以**控制块计数本身**是线程安全的。
但这不等于“对象访问线程安全”，多个线程同时通过同一个 `shared_ptr` 访问对象内容，仍然需要额外加锁。

------

### 16. 为什么 `shared_ptr` 会有循环引用？怎么解决？

因为两个对象如果互相持有对方的 `shared_ptr`，那么双方引用计数都至少为 1。
即使外部都不再使用它们，这两个对象也不会析构，因为彼此都还在“引用”对方。
这就形成循环引用，最终导致内存泄漏。
解决方法是把其中一个方向改成 `weak_ptr`，因为弱引用不增加引用计数。
经典场景是父子节点、双向链表、观察者模式。

------

### 17. 虚函数的作用是什么？

虚函数的作用是支持**运行时多态**。
当通过父类指针或引用调用虚函数时，程序会根据对象的真实类型，决定调用父类版本还是子类重写版本。
这样就能实现统一接口下的不同行为。
没有虚函数时，函数调用在编译期就已经确定了，不能实现真正的动态分派。

------

### 18. 虚函数表是怎么实现多态的？

包含虚函数的类，编译器通常会为它生成一张**虚函数表 vtable**。
对象内部通常会有一个隐藏的虚表指针 `vptr`，指向所属类的虚函数表。
当通过父类指针调用虚函数时，程序会先通过对象里的 `vptr` 找到实际类型对应的虚表，再取出正确函数地址调用。
因此多态的本质是“运行时查表调用”。

------

### 19. 构造函数可以是虚函数吗？析构函数为什么常常要写成虚函数？

构造函数不能是虚函数。
因为调用构造函数时，对象本身都还没有完全构造出来，虚表指针都未必稳定，所以没有意义也无法实现。
析构函数常常要写成虚函数，是为了保证通过**父类指针删除子类对象**时，能先调用子类析构，再调用父类析构。
否则可能只执行父类析构，导致子类资源泄漏。
一句标准话术：只要类可能被继承并且会通过父类指针释放，就应该把析构函数写成虚函数。

------

### 20. `static` 的作用有哪些？

`static` 在不同位置作用不同。

- 修饰局部变量：生命周期变为整个程序运行期，但作用域仍在函数内。
- 修饰全局变量或函数：限制链接属性，使其只在当前文件可见。
- 修饰类成员变量：属于类本身，不属于某个对象，所有对象共享一份。
- 修饰类成员函数：没有 `this` 指针，只能访问静态成员。

所以 `static` 主要关联三个词：**生命周期延长、类共享、文件级可见性控制**。

------

### 21. `inline` 的作用是什么？一定会内联吗？

`inline` 主要有两个作用。
第一，建议编译器把函数展开到调用处，减少函数调用开销。
第二，更重要的是允许函数定义放在头文件里，避免多重定义冲突。
但 `inline` 只是建议，不保证一定内联。
是否真正展开，取决于编译器优化策略、函数复杂度、是否递归等因素。

------

### 22. `vector` 的底层实现是什么？扩容机制是什么？

`vector` 底层是**一段连续内存**，类似动态数组。
它有三个关键概念：`size` 表示当前元素个数，`capacity` 表示当前容量，底层用指针管理连续空间。
当容量不够时，会重新申请一块更大的连续内存，把旧元素搬过去，再释放旧空间。
扩容策略没有强制标准，不同实现略有差异，但常见是按 1.5 倍或 2 倍增长。
连续存储的好处是随机访问快，缓存友好；缺点是中间插入删除开销大，扩容会导致搬迁。

------

### 23. `vector` 和 `list` 的区别是什么？

`vector` 底层是连续数组，`list` 底层是双向链表。
`vector` 支持 O(1) 随机访问，缓存命中率高，尾插通常高效；但中间插入删除可能要移动大量元素。
`list` 插入删除某个节点很快，只要拿到迭代器就是 O(1)；但不支持随机访问，额外指针开销大，局部性差。
实际工程里大多数场景优先用 `vector`，因为现代 CPU 下连续内存带来的性能优势通常更明显。

------

### 24. `map` 和 `unordered_map` 的区别是什么？

`map` 底层一般是红黑树，键有序，查找、插入、删除复杂度通常是 O(log n)。
`unordered_map` 底层是哈希表，键无序，平均查找、插入、删除复杂度是 O(1)，最坏可能退化到 O(n)。
`map` 适合需要有序遍历、范围查询、上下界查找的场景。
`unordered_map` 适合只关心快速查找，不关心顺序的场景。
面试里最好补一句：理论上 `unordered_map` 更快，但实际性能还跟哈希质量、冲突情况、扩容次数有关。

------

### 25. `unordered_map` 为什么快？它的底层是什么？

`unordered_map` 快的原因是它使用哈希函数把 key 映射到桶中，查找时不需要像树那样逐层比较，而是直接定位到桶，再在桶里找目标元素。
平均情况下时间复杂度是 O(1)。
底层一般是**哈希表 + 桶数组**，发生冲突时可能采用链地址法或其他实现策略。
不过如果哈希函数不好、冲突严重，性能会退化。

------

### 26. 哈希冲突怎么解决？

常见方法有两类。
第一类是**开放定址法**，冲突时继续找下一个空槽，比如线性探测、二次探测。
第二类是**链地址法**，每个桶后面挂链表或其他结构，冲突元素放到同一个桶里。
C++ 标准库里的 `unordered_map` 通常采用链地址法思想。
此外，还可以通过更好的哈希函数、合理负载因子、及时 rehash 来减少冲突。

------

### 27. `set` / `map` 为什么一般是红黑树而不是 AVL？

因为红黑树在保持“基本平衡”的同时，旋转次数更少，插入删除时的调整成本更低，更适合频繁修改的通用容器场景。
AVL 树更严格平衡，所以查询时理论上更快一些，但插入删除时维护成本更高。
标准库容器需要在查找、插入、删除之间做综合权衡，所以一般选红黑树。
一句话：AVL 更“追求查找极致”，红黑树更“追求综合稳定”。

------

### 28. `auto` 和 `decltype` 的区别是什么？

`auto` 是根据初始化表达式推导变量类型。
比如 `auto x = 10;`，推导出 `int`。
`decltype` 是根据表达式本身推导类型，但**不计算表达式**。
比如 `decltype(x) y;`，`y` 的类型和 `x` 一样。
`auto` 更适合定义变量，`decltype` 更适合保留复杂表达式精确类型，尤其是模板和泛型编程里。

------

### 29. 模板是什么？模板和宏有什么区别？

模板是 C++ 的泛型编程机制，可以让同一套代码适配不同类型，编译器在编译期进行类型替换和实例化。
宏是预处理阶段的文本替换，不理解类型，也不做语义检查。
模板有类型安全、作用域规则、可调试性更好等优点；宏容易出错，比如重复求值、优先级问题。
所以在现代 C++ 里，能用模板、常量表达式、内联函数解决的，通常不建议再用宏。

------

### 30. 什么是函数模板和类模板？

函数模板是函数参数类型泛化，比如 `template<typename T> T add(T a, T b)`。
类模板是把整个类参数化，比如 `vector<T>`、`map<K,V>`。
函数模板适合封装通用算法，类模板适合封装通用数据结构。
实例化时，函数模板有时能自动推导类型，类模板通常要显式指定模板参数，或者依赖类模板参数推导。

------

### 31. `nullptr` 和 `NULL` 的区别是什么？

`NULL` 本质上通常是一个宏，很多实现里就是 `0` 或 `((void*)0)`。
在 C++ 中它可能会引发重载歧义，因为它更像整数常量。
`nullptr` 是 C++11 引入的关键字，专门表示空指针，类型是 `std::nullptr_t`，能更准确地区分整数和指针。
所以现代 C++ 应优先使用 `nullptr` 而不是 `NULL`。

------

### 32. C++11 你用过哪些新特性？

常见可以答这些：

- `auto`、`decltype`
- 右值引用和移动语义
- 智能指针
- lambda 表达式
- `nullptr`
- 范围 `for`
- `unordered_map`
- `thread`、`mutex`、`condition_variable`
- `atomic`
- `override`、`final`
- 列表初始化
- `constexpr`

面试里最好别只背名字，要顺手说两三个你最熟的使用场景，比如“容器扩容时移动语义减少拷贝”“多线程里用 `lock_guard` 管锁”。

------

### 33. `volatile` 是干什么的？它能保证线程安全吗？

`volatile` 告诉编译器：这个变量的值可能在程序控制之外被修改，所以每次访问都不要随意缓存到寄存器或做过度优化。
它常见于硬件寄存器、信号处理、某些内存映射 IO 场景。
但它**不能保证线程安全**，也不能保证原子性、可见性顺序等完整并发语义。
多线程同步应该使用 `atomic`、锁、内存序等机制，而不是依赖 `volatile`。

------

### 34. `sizeof` 和 `strlen` 的区别是什么？

`sizeof` 是编译期运算符，用来求类型或对象占用的字节数。
`strlen` 是运行时库函数，用来求 C 风格字符串长度，统计到第一个 `\0` 为止，不包含 `\0`。
比如字符数组 `char s[] = "abc";`，`sizeof(s)` 是 4，因为包含结尾的 `\0`；`strlen(s)` 是 3。
另外，对指针做 `sizeof` 得到的是指针大小，不是它指向内容的长度。

------

### 35. 异常处理机制 `try-catch-throw` 的底层原理了解吗？

面试里可以这样答：
C++ 异常机制本质上依赖编译器和运行时支持。
`throw` 抛出异常后，程序会沿调用栈向上查找匹配的 `catch`，这个过程叫**栈展开**。
在栈展开过程中，当前作用域内已构造对象的析构函数会被自动调用，这也是 RAII 在异常场景下依然安全的重要原因。
异常机制会带来一定运行时和二进制体积成本，所以很多高性能系统里会谨慎使用。

------

### 36. `mutex`、`lock_guard`、`unique_lock` 的区别是什么？

`mutex` 是底层互斥锁对象，只提供加锁解锁接口，需要手动管理。
`lock_guard` 是一个轻量级 RAII 封装，构造时加锁，析构时自动解锁，适合简单场景。
`unique_lock` 更灵活，支持延迟加锁、手动解锁再加锁、配合条件变量使用，但开销略大。
简单说：裸 `mutex` 太容易忘记释放，通常配合 RAII 用；普通场景优先 `lock_guard`，复杂场景用 `unique_lock`。

------

### 37. 死锁产生的条件是什么？怎么避免？

死锁有四个必要条件：

1. 互斥
2. 请求并保持
3. 不可剥夺
4. 循环等待

避免方法常见有：

- 固定加锁顺序，破坏循环等待
- 一次性申请全部资源
- 使用 `std::lock` 同时加多个锁
- 缩小锁粒度和持锁时间
- 尽量避免嵌套锁

面试里最常见的一句是：**统一加锁顺序是工程里最实用的死锁规避方法**。

------

### 38. 线程同步有哪些方式？

常见线程同步方式有：

- 互斥锁 `mutex`
- 读写锁
- 条件变量 `condition_variable`
- 信号量
- 原子变量 `atomic`
- 自旋锁
- 屏障等同步原语

如果面试官继续追问区别，可以说：
互斥锁保护临界区；条件变量用于线程间等待和通知；原子变量适合简单共享状态；读写锁适合读多写少场景。

------

### 39. 进程和线程在 C++ 开发里分别怎么用？

线程更适合一个进程内部的并发任务，因为线程共享地址空间，通信成本低，切换开销相对小。
在 C++ 后台开发里，线程常用于线程池、网络 IO、任务并行、异步处理。
进程隔离性更强，一个进程崩了通常不直接拖垮别的进程，适合服务拆分、独立模块、跨语言组件。
所以一般是：**同一服务内部用线程，服务边界或强隔离场景用进程**。

------

### 40. 你项目里最常用的 STL 容器是什么？为什么？

面试里比较稳妥的答法是：**最常用的是 `vector` 和 `unordered_map`**。
`vector` 因为连续内存、随机访问快、缓存友好，绝大多数顺序存储场景都适合。
`unordered_map` 因为按 key 查找快，适合做索引、缓存、状态表。
如果想显得更工程化，可以补一句：
“除非明确需要有序性、频繁中间插入删除或范围查询，否则我一般优先考虑 `vector` 和 `unordered_map`。”

### 41. 红黑树的性质是什么？

红黑树是一种自平衡二叉搜索树，主要性质有：

1. 每个节点不是红色就是黑色
2. 根节点是黑色
3. 所有叶子空节点视为黑色
4. 红节点的两个孩子必须是黑色，也就是不能有连续红节点
5. 从任一节点到其所有后代叶子节点的路径上，黑色节点数相同

这些性质保证了树不会退化得太严重，从而让查找、插入、删除保持 O(log n)。

------

### 42. `emplace_back` 和 `push_back` 的区别是什么？

`push_back` 是把一个已经存在的对象拷贝或移动到容器尾部。
`emplace_back` 是直接在容器尾部原地构造对象，把构造参数直接传进去。
对于复杂对象，`emplace_back` 可以少一次临时对象构造和拷贝/移动，效率更高。
但如果传进去的本来就是一个现成对象，两者差异可能不大。
一句话：`push_back` 偏“塞进去”，`emplace_back` 偏“原地造出来”。

------

### 43. `reserve` 和 `resize` 的区别是什么？

`reserve(n)` 是预留容量，把 `capacity` 至少扩到 `n`，但**不改变 `size`**，也不会创建新元素。
`resize(n)` 是直接修改元素个数 `size`。
如果变大，会新增元素并进行默认初始化；如果变小，会销毁多余元素。
所以 `reserve` 是“提前扩容防止反复搬迁”，`resize` 是“真改容器里元素个数”。

------

### 44. `vector` 迭代器失效场景有哪些？

`vector` 迭代器失效最常见有两类：

1. **扩容时**：底层内存整体搬迁，原有所有迭代器、引用、指针都失效
2. **中间插入/删除时**：插入点或删除点及其之后的迭代器通常失效

尾部 `push_back` 在不扩容时，一般之前的迭代器仍有效；但一旦扩容，就全失效。
所以使用 `vector` 时要特别警惕扩容带来的悬挂迭代器问题。

------

### 45. `map` / `unordered_map` 的迭代器失效情况？

`map` 底层是红黑树，插入元素一般不会导致已有迭代器失效；删除某个元素时，只有指向被删元素的迭代器失效，其它通常仍有效。
`unordered_map` 插入时如果触发 rehash，可能导致**所有迭代器失效**；删除某个元素时，指向该元素的迭代器失效。
所以 `unordered_map` 比 `map` 更需要小心 rehash 带来的迭代器问题。

------

### 46. `static_cast`、`dynamic_cast`、`const_cast`、`reinterpret_cast` 的区别是什么？

- `static_cast`：常规类型转换，编译期可检查，常用于基本类型转换、上行转换等
- `dynamic_cast`：主要用于继承体系中的安全向下转型，依赖 RTTI，失败时指针返回 `nullptr`
- `const_cast`：用于去掉或添加 `const` / `volatile` 属性
- `reinterpret_cast`：底层强制解释类型转换，最不安全，通常只在底层系统编程里使用

面试里一定要强调：`reinterpret_cast` 风险最大，要慎用。

------

### 47. lambda 表达式底层是什么？

lambda 本质上可以理解成编译器生成的一个**匿名函数对象**。
它会生成一个带 `operator()` 的闭包类。
捕获的变量会变成这个闭包对象的成员。
所以 lambda 并不神秘，本质就是“语法糖 + 函数对象”。
无捕获 lambda 甚至可以退化成函数指针使用。

------

### 48. `std::function` 和函数指针的区别？

函数指针只能指向普通函数或静态成员函数，类型比较单一。
`std::function` 是一个通用可调用对象包装器，可以存普通函数、lambda、仿函数、`bind` 结果、成员函数适配对象等。
它更灵活，但会有一定类型擦除和封装开销。
所以性能敏感场景可能优先函数指针或模板；需要统一抽象回调接口时，`std::function` 很方便。

------

### 49. 条件变量 `condition_variable` 的使用场景是什么？

条件变量用于线程之间的**等待与通知**。
典型场景是生产者消费者模型：消费者线程在队列为空时等待，生产者放入数据后通知消费者继续执行。
它通常和 `mutex + unique_lock` 一起使用，因为等待时需要原子地“释放锁并休眠”，被唤醒后再重新加锁。
条件变量能避免忙等，减少 CPU 空转。

------

### 50. 原子变量 `atomic` 和锁的区别是什么？

`atomic` 适合单个变量的原子读改写，比如计数器、自旋标记、状态位。
它不需要线程进入内核阻塞，开销通常比锁小。
但它只能保证某些简单操作的原子性，不适合保护一大段复杂临界区。
锁适合保护多个变量和复杂逻辑，表达能力更强，但开销更高，也可能阻塞。
一句话：**简单共享状态优先 atomic，复杂共享资源优先锁**。

------

### 51. 内存对齐是什么？为什么要内存对齐？

内存对齐是指数据在内存中的起始地址满足某种边界要求，比如 4 字节类型按 4 的倍数地址存放。
这样做主要是为了提高 CPU 访问效率，因为很多硬件读取对齐数据更快，甚至有些平台对未对齐访问有限制。
结构体中为了满足对齐规则，编译器可能会插入填充字节，所以结构体大小往往不等于成员大小简单相加。
面试里常追问：结构体大小要考虑**成员对齐**和**整体对齐**。

------

### 52. 什么是野指针？什么是悬空指针？

野指针通常指未初始化就使用的指针，它指向随机地址，访问会产生未定义行为。
悬空指针指原本指向有效内存，但那块内存已经被释放或失效了，指针还保留旧地址。
两者都会带来严重 bug。
常见规避方法是：指针定义时初始化为 `nullptr`，释放后及时置空，尽量使用智能指针替代裸指针。

------

### 53. 构造函数初始化列表为什么更推荐？

因为成员变量在进入构造函数体之前，就已经开始初始化了。
对于 `const` 成员、引用成员、没有默认构造函数的成员对象，必须使用初始化列表。
另外，对成员对象来说，初始化列表是“直接构造”，而在函数体里赋值往往是“先默认构造再赋值”，效率更低。
所以面试里标准结论是：**初始化列表不仅更高效，而且某些成员场景下是必须的**。

------

### 54. 单例模式怎么写？线程安全怎么保证？

现代 C++ 里最推荐的写法是**函数内静态局部变量**：

```cpp
class Singleton {
public:
    static Singleton& instance() {
        static Singleton obj;
        return obj;
    }
private:
    Singleton() = default;
    Singleton(const Singleton&) = delete;
    Singleton& operator=(const Singleton&) = delete;
};
```

C++11 之后，函数内局部静态变量的初始化是线程安全的。
这种写法简单、懒加载、无需手动加锁。
面试里比“双重检查锁”更稳，也更推荐。

------

### 55. 工厂模式、观察者模式、生产者消费者模型了解吗？

可以这样简答：

- **工厂模式**：把对象创建逻辑封装起来，调用方不直接 `new` 具体类，降低耦合，便于扩展
- **观察者模式**：一个对象状态变化时通知多个订阅者，典型场景是事件系统、消息通知
- **生产者消费者模型**：多个生产者往缓冲区放数据，多个消费者取数据处理，核心是队列、锁、条件变量

如果面试官继续问，你可以再结合后台开发场景说：
“工厂模式常用于策略对象创建，观察者模式用于事件分发，生产者消费者模型常用于日志、任务队列、异步处理。”

------

## Linux

### 1. Linux 下查看进程常用哪些命令？

最常用的是 `ps`、`top`、`htop`、`pgrep`。
`ps -ef` 或 `ps aux` 用来看某一时刻的进程快照。
`top` 和 `htop` 用来动态查看进程的 CPU、内存占用。
`pgrep` 可以按进程名快速查 PID。
排障时我一般会先用 `ps` 定位，再用 `top` 看资源占用。

------

### 2. `ps -ef` 和 `ps aux` 有什么区别？

两者本质上都是查看进程信息，只是风格不同。
`ps -ef` 是 **System V 风格**，常见字段有 UID、PID、PPID、CMD。
`ps aux` 是 **BSD 风格**，常见字段有 USER、%CPU、%MEM、COMMAND。
实际使用上都很常见，很多人会配合 `grep` 用，比如 `ps -ef | grep nginx`。
面试里可以说：区别主要是参数风格和显示字段略有不同，核心用途差不多。

------

### 3. `top` 你怎么看 CPU 和内存占用？

`top` 里进程级别常看 `%CPU` 和 `%MEM`。
系统级别会看 load average、Cpu 使用拆分、内存和 swap 情况。
CPU 那行里常看 `us`、`sy`、`id`、`wa`：

- `us`：用户态 CPU
- `sy`：内核态 CPU
- `id`：空闲 CPU
- `wa`：IO 等待

内存一般看总内存、已用、空闲、buff/cache。
如果 `%CPU` 很高，说明进程吃 CPU；如果 `wa` 很高，说明可能是 IO 瓶颈。

------

### 4. 怎么查某个端口被谁占用了？

最常用的是：

```bash
lsof -i :8080
ss -lntp | grep 8080
netstat -lntp | grep 8080
```

`lsof -i` 能直接看到哪个进程占用了端口。
`ss` 比 `netstat` 更现代、速度更快。
线上排障时，我通常优先用 `ss` 或 `lsof`。

------

### 5. `netstat`、`ss`、`lsof` 分别怎么用？

`netstat` 用于查看网络连接、端口监听、路由信息，但现在很多场景更推荐 `ss`。
`ss` 是 socket statistics，查看连接状态更快，比如：

```bash
ss -lntp
ss -antp
```

`lsof` 是查看“打开的文件”，而在 Linux 里 socket 也算文件，所以它也能查端口和进程关系。
一句话：
看连接和监听常用 `ss`，看“哪个进程占了哪个端口”常用 `lsof`。

------

### 6. 如何杀掉一个进程？

可以先查到 PID，再用 `kill`：

```bash
kill PID
```

如果进程不响应，再用强制信号：

```bash
kill -9 PID
```

也可以按进程名用 `pkill` 或 `killall`。
但线上一般优先普通 `kill`，因为 `kill -9` 太暴力，可能导致来不及清理资源。

------

### 7. `kill` 和 `kill -9` 的区别是什么？

`kill` 默认发送的是 `SIGTERM`，表示“请求进程正常退出”。
进程可以捕获这个信号，做资源清理、保存状态、优雅关闭。
`kill -9` 发送的是 `SIGKILL`，不能被捕获或忽略，内核会直接强制终止进程。
所以 `kill -9` 虽然有效，但可能带来脏数据、临时文件未清理、锁未释放等问题。
工程上通常是：**先 TERM，再不得已才 KILL**。

------

### 8. Linux 下如何查看日志？

常见是用 `cat`、`less`、`tail`、`grep`、`journalctl`。
如果日志很大，一般不用 `cat`，而是：

```bash
tail -f app.log
grep "ERROR" app.log
less app.log
```

如果是 systemd 管理的服务，可以用：

```bash
journalctl -u nginx.service
```

线上我最常用的是 `tail -f` 实时看日志，再配合 `grep` 过滤关键字。

------

### 9. `tail -f`、`grep`、`awk`、`sed` 常见用法是什么？

- `tail -f`：实时追踪日志末尾新增内容
- `grep`：按关键字过滤文本，比如查错误行
- `awk`：按列处理文本，适合做字段提取和统计
- `sed`：流式编辑，适合替换、删除、批量修改文本

例如：

```bash
tail -f app.log
grep "timeout" app.log
awk '{print $1, $NF}' app.log
sed 's/old/new/g' file.txt
```

面试里可以说：`grep` 偏过滤，`awk` 偏结构化处理，`sed` 偏文本编辑。

------

### 10. 如何查找文件？

最常见是 `find` 和 `locate`。
`find` 按路径、名字、时间、大小等条件实时查找，功能最强：

```bash
find /var/log -name "*.log"
```

`locate` 基于数据库查找，速度快，但结果可能不是最新。
工程里更常用 `find`，因为更准确灵活。

------

### 11. `find` 和 `grep` 的区别是什么？

`find` 是查**文件**，根据文件名、路径、时间、大小等元信息找文件。
`grep` 是查**文件内容**，根据关键字在文本里匹配。
比如：

```bash
find . -name "*.cpp"
grep -r "main" .
```

前者找文件名，后者找内容。
面试里一句话：**find 查“文件在哪”，grep 查“内容里有什么”**。

------

### 12. 如何查看磁盘使用情况？

最常见是 `df` 和 `du`。
`df -h` 看文件系统整体磁盘使用率。
`du -sh` 看某个目录实际占了多少空间。
例如：

```bash
df -h
du -sh /var/log
```

排查磁盘满时，一般先 `df -h` 看哪个分区满了，再 `du` 往下定位大目录。

------

### 13. `df` 和 `du` 的区别是什么？

`df` 看的是**文件系统维度**的磁盘使用情况，比如某个挂载分区总共多大、用了多少。
`du` 看的是**目录或文件维度**占用了多少空间。
所以 `df` 更适合判断“磁盘满没满”，`du` 更适合找“到底哪个目录占得多”。
面试里常用一句：`df` 看盘，`du` 看目录。

------

### 14. 如何查看内存使用情况？

最常用：

```bash
free -h
top
cat /proc/meminfo
```

`free -h` 适合快速看总内存、可用内存、swap。
`top` 可以看进程级内存占用。
`/proc/meminfo` 更详细，适合进一步分析。
一般我会先用 `free -h` 看全局，再用 `top` 找大内存进程。

------

### 15. `free -h` 里各字段是什么意思？

常见字段有：

- `total`：总内存
- `used`：已使用内存
- `free`：完全空闲内存
- `buff/cache`：缓存和缓冲区占用
- `available`：可供新进程使用的大致内存

面试里要特别说明：
Linux 会尽量利用空闲内存做 page cache，所以 `free` 很小不一定表示真的内存紧张，更应该看 `available`。
这是个很常见的追问点。

------

### 16. Linux 文件权限 `rwx` 分别是什么意思？

`r` 是读权限，`w` 是写权限，`x` 是执行权限。
权限通常分三组：文件所有者、所属组、其他用户。
比如 `-rwxr-xr--` 表示：

- 所有者：读写执行
- 同组用户：读执行
- 其他用户：只读

目录上的权限含义稍有不同，尤其 `x` 对目录表示“可进入、可遍历”。

------

### 17. `chmod 755` 表示什么？

`755` 是八进制权限写法，对应：

- 所有者：7 = 4+2+1 = rwx
- 同组用户：5 = 4+1 = r-x
- 其他用户：5 = 4+1 = r-x

所以 `chmod 755 file` 表示所有者可读写执行，其他人可读可执行。
这是可执行脚本和程序的常见权限设置。

------

### 18. 硬链接和软链接的区别是什么？

硬链接本质上是多个文件名指向同一个 inode。
软链接本质上是一个特殊文件，里面存的是目标路径。
区别主要有：

- 硬链接和原文件共享 inode；软链接有自己独立 inode
- 硬链接一般不能跨文件系统；软链接可以
- 硬链接通常不能链接目录；软链接可以
- 删除原文件后，硬链接仍可访问数据；软链接会变成悬挂链接

一句话：硬链接更像“同一个文件的别名”，软链接更像“快捷方式”。

------

### 19. inode 是什么？

inode 是 Linux 文件系统中用于描述文件元信息的数据结构。
它保存文件大小、权限、属主、时间戳、数据块位置等信息，但**不保存文件名**。
文件名其实存放在目录项里，目录项负责把“文件名”和“inode 编号”对应起来。
所以一个文件可以有多个文件名，也就是多个硬链接，因为它们可以指向同一个 inode。

------

### 20. Linux 下一个文件删除了，磁盘空间为什么可能没有释放？

因为文件删除只是把目录项去掉，并减少链接计数。
如果还有进程持有这个文件的打开句柄，那么对应的数据块不会立即释放。
所以会出现“文件删了，但磁盘空间还没回来”的现象。
这时可以用 `lsof | grep deleted` 查看哪个进程还占着这个已删除文件。
这是线上日志文件常见问题。

------

### 21. 什么是僵尸进程？什么是孤儿进程？

僵尸进程是子进程已经退出，但父进程还没有调用 `wait` 或 `waitpid` 回收其退出状态，所以在进程表里保留一个僵尸项。
它本身不占多少内存，但会占 PID 和进程表项。
孤儿进程是父进程先退出了，子进程还在运行，这时子进程会被 `init` 或 systemd 接管。
一句话：**僵尸是“死了没收尸”，孤儿是“活着但没爹了”**。

------

### 22. 怎么查看 CPU 负载？load average 是什么？

可以用：

```bash
uptime
top
w
```

`load average` 表示最近 1 分钟、5 分钟、15 分钟内处于**可运行状态和不可中断状态**任务的平均数。
它不等于 CPU 使用率。
如果机器是 4 核，load 长期接近 4 说明负载比较满；明显大于 4 说明任务排队严重。
所以要结合 CPU 核数来看 load 是否异常。

------

### 23. 用户态和内核态的区别是什么？

用户态是普通应用程序运行的模式，权限受限，不能直接访问硬件和内核关键资源。
内核态权限高，可以执行特权指令、操作硬件、管理内存和调度。
应用程序需要进行文件读写、网络 IO、进程管理等操作时，通常要通过**系统调用**切换到内核态。
频繁切换用户态和内核态会带来性能开销。

------

### 24. Linux 下进程间通信方式有哪些？

常见 IPC 方式有：

- 管道 pipe
- 命名管道 FIFO
- 消息队列
- 共享内存
- 信号量
- 信号
- socket
- mmap

如果是同机父子进程，管道很常见；
如果追求高性能大数据交换，共享内存很常见；
如果是跨机器通信，一般就用 socket。

------

### 25. 管道和消息队列、共享内存的区别是什么？

管道适合简单的、通常是有亲缘关系进程间的单向字节流通信，使用简单，但灵活性一般。
消息队列是按“消息”为单位通信，支持消息边界，适合结构化传输。
共享内存是多个进程映射同一块物理内存，速度最快，因为少了一次数据拷贝，但需要自己处理同步问题。
一句话：**共享内存最快，消息队列更结构化，管道最简单**。

------

### 26. `select`、`poll`、`epoll` 的区别是什么？

三者都是 IO 多路复用机制。
`select` 用位图保存 fd，数量有限，且每次调用都要把整个 fd 集合从用户态拷到内核态，还要线性扫描。
`poll` 用链表或数组描述 fd，没有 `select` 的固定上限问题，但本质上还是线性扫描。
`epoll` 把 fd 注册到内核，由内核维护就绪队列，返回时只通知活跃 fd，因此在高并发场景更高效。
一句话：`select/poll` 更像“每次全量问一遍”，`epoll` 更像“谁好了谁通知我”。

------

### 27. `epoll` 为什么高效？

主要有两个原因。
第一，`epoll` 不需要每次把整个 fd 集合反复从用户态拷贝到内核态，fd 注册一次即可。
第二，`epoll_wait` 返回的是**就绪事件集合**，不需要像 `select/poll` 那样每次遍历全部 fd。
所以当连接很多、活跃连接相对少时，`epoll` 优势非常明显。
这也是高并发网络服务器常用 `epoll` 的原因。

------

### 28. 阻塞 IO、非阻塞 IO、IO 多路复用分别是什么？

阻塞 IO 指调用读写接口时，如果数据没准备好，线程会一直阻塞等待。
非阻塞 IO 指如果数据没准备好，立即返回错误码或空结果，不会阻塞线程。
IO 多路复用指一个线程同时监听多个 fd，当某个 fd 就绪时再处理。
它本质上不是“数据复制方式变了”，而是“等待多个 IO 的方式更高效了”。

------

### 29. Linux 下怎么排查线上服务 CPU 飙高？

常见步骤是：

1. `top` 看哪个进程 CPU 高
2. `top -H -p PID` 看具体哪个线程高
3. 把线程 ID 转成 16 进制，对照 `jstack` 或 `gdb`、`pstack` 看调用栈
4. 结合日志判断是否死循环、频繁重试、锁竞争、热点计算等

如果是 C/C++ 服务，也可以配合 `perf` 分析热点函数。
面试里这种题目一定要答出“**先定位进程，再定位线程，再定位函数栈**”。

------

### 30. Linux 下怎么排查内存泄漏或内存暴涨？

先用 `top`、`ps aux --sort=-%mem` 看哪个进程内存异常。
然后区分是正常缓存增长、业务数据堆积，还是疑似泄漏。
C/C++ 程序常用 `valgrind`、`asan`、`heaptrack` 等工具定位内存泄漏。
也可以通过监控 RSS、堆对象数量、连接数、缓存命中等指标辅助判断。
排查思路通常是：**先确认增长趋势，再定位对象类型，再回到代码看谁申请了没释放**。

### 31. Shell 脚本基础会问到哪些？

常见会问：

- 变量怎么定义和使用
- 条件判断 `if`
- 循环 `for`、`while`
- 参数传递 `$1`、`$#`、`$?`
- 管道和重定向
- 如何执行一个 shell 脚本

比如：

```bash
#!/bin/bash
echo $1
if [ -f test.txt ]; then
  echo "exists"
fi
```

后台开发岗一般不会问太深，但至少要能看懂简单运维脚本。

------

### 32. `nohup`、`&`、`screen`、`tmux` 的区别？

`&` 是把命令放到后台执行，但终端关闭后进程可能一起挂掉。
`nohup` 是让进程忽略挂断信号，终端断开后仍可继续运行。
`screen` 和 `tmux` 是终端复用工具，可以新建会话、断开后再恢复，功能比 `nohup` 更强。
工程里临时后台跑任务常用 `nohup command &`，长期交互式调试更适合 `tmux`。

------

### 33. 环境变量怎么查看和设置？

查看可以用：

```bash
env
printenv
echo $PATH
```

临时设置：

```bash
export MY_VAR=123
```

永久设置一般写到 `~/.bashrc`、`~/.zshrc` 或系统配置文件里。
环境变量常用于程序配置、PATH 路径、动态库路径等。

------

### 34. `export` 是做什么的？

`export` 用于把 shell 变量导出为环境变量，使其对子进程可见。
比如：

```bash
MY_VAR=123
export MY_VAR
```

如果不 `export`，它只是当前 shell 的局部变量；
`export` 后，当前 shell 启动的子进程也能读到。
这是 shell 面试里很常见的点。

------

### 35. Linux 启动一个程序的流程大致是什么？

可以简化为：

1. shell 解析命令
2. 调用 `fork` 创建子进程
3. 子进程调用 `execve` 加载可执行文件
4. 内核加载 ELF 文件、建立虚拟地址空间、映射代码段数据段
5. 动态链接器加载依赖库
6. 设置栈、堆、环境变量、参数
7. 跳转到程序入口开始执行

面试里不需要讲太细，但说出 `fork + execve + 装载 ELF + 动态链接` 通常就够了。

------

### 36. 动态链接库和静态链接库的区别是什么？

静态链接库在编译链接阶段就把代码拷贝进可执行文件，所以生成的程序更大，但运行时不依赖外部库文件。
动态链接库在运行时再加载，多个程序可以共享同一份库，节省磁盘和内存，也便于统一升级。
缺点是运行时依赖环境，可能出现“库找不到”或版本不兼容问题。
一句话：静态库更独立，动态库更灵活。

------

### 37. `ldd` 是干什么的？

`ldd` 用于查看一个可执行文件依赖了哪些动态链接库。
例如：

```bash
ldd ./a.out
```

它常用于排查“程序启动失败是不是因为缺库”这类问题。
如果看到某个 so 显示 `not found`，通常就说明动态库路径或安装有问题。

------

### 38. `strace` 和 `gdb` 用过吗？

`strace` 主要看进程执行了哪些系统调用，比如文件打开失败、网络连接失败、权限错误等，很适合排查程序“卡在哪个系统调用上”。
`gdb` 是调试器，可以断点、单步、查看栈、变量、内存，适合定位崩溃和逻辑错误。
一句话：`strace` 偏系统调用层排障，`gdb` 偏代码级调试。

------

### 39. 页缓存 page cache 是什么？

page cache 是内核用空闲内存缓存磁盘文件数据的机制。
这样同一文件再次读取时，可以直接从内存返回，减少磁盘 IO。
它是 Linux 文件系统性能优化的重要手段。
所以很多时候内存看起来“被占满”，其实相当一部分是 page cache，可以在需要时回收。
这也是为什么看内存不能只看 `free`，要看 `available`。

------

### 40. 零拷贝在 Linux 里怎么实现？

零拷贝的目标是减少数据在内核态和用户态之间、以及内存缓冲区之间的不必要复制。
Linux 常见方式有：

- `sendfile`
- `mmap`
- `splice`

比如文件传输场景下，`sendfile` 可以让数据直接在内核缓冲区和网卡之间流转，减少用户态参与。
这样能降低 CPU 开销，提高吞吐。
高性能服务器和文件传输场景里经常会用到这个概念。

------

## Docker

### 1. Docker 是什么？解决了什么问题？

Docker 是一种容器化技术，用来把应用和它的运行环境一起打包、分发和运行。
它主要解决的是“开发、测试、生产环境不一致”以及应用部署复杂的问题。
有了 Docker，应用连同依赖库、配置、运行时环境可以一起封装成镜像，到哪里运行都比较一致。
所以它的核心价值可以概括成三点：**环境一致、交付标准化、部署更轻量**。

------

### 2. Docker 和虚拟机的区别是什么？

虚拟机是在宿主机上虚拟出一整套硬件，再跑一个完整的客户机操作系统。
Docker 容器不需要单独的 Guest OS，而是和宿主机共享内核，只隔离进程、文件系统、网络等资源。
所以容器比虚拟机更轻量、启动更快、资源利用率更高。
但虚拟机隔离性通常更强，因为它连内核都是独立的。
一句话：**虚拟机是“虚拟一台机器”，Docker 是“隔离一个进程环境”**。

------

### 3. 镜像和容器的区别是什么？

镜像是一个只读模板，里面包含应用代码、依赖、基础环境、启动配置等。
容器是镜像运行起来后的实例。
可以理解成：镜像像类，容器像对象；镜像是静态的，容器是动态运行的。
同一个镜像可以启动多个容器，每个容器都有自己的运行状态和可写层。

------

### 4. Docker 的底层原理了解吗？

Docker 底层核心依赖 Linux 的 **namespace** 和 **cgroup**。
namespace 用来做资源隔离，比如进程、网络、挂载点、用户、IPC 等隔离。
cgroup 用来做资源限制和统计，比如限制 CPU、内存、IO。
再配合联合文件系统实现镜像分层和容器可写层。
所以 Docker 本质上不是虚拟机，而是 Linux 内核能力上的工程化封装。

------

### 5. Docker 为什么比虚拟机轻量？

因为 Docker 容器不需要虚拟硬件，也不需要单独再装一个完整操作系统。
它直接复用宿主机内核，只把应用运行所需的进程环境隔离出来。
这样就省去了 Guest OS 的启动和维护成本。
所以容器通常启动只要秒级甚至更快，而虚拟机往往更重、更慢。
轻量的本质原因就是：**共享宿主机内核**。

------

### 6. `Dockerfile` 常见指令有哪些？

常见指令有：

- `FROM`：指定基础镜像
- `RUN`：构建镜像时执行命令
- `COPY`：拷贝文件到镜像
- `ADD`：类似 COPY，但功能更多
- `WORKDIR`：设置工作目录
- `ENV`：设置环境变量
- `EXPOSE`：声明容器监听端口
- `CMD`：指定默认启动命令
- `ENTRYPOINT`：指定容器入口命令

如果面试官追问，我一般会重点展开 `FROM`、`RUN`、`COPY`、`CMD`、`ENTRYPOINT`。

------

### 7. `CMD` 和 `ENTRYPOINT` 的区别是什么？

`CMD` 是给容器提供**默认执行命令或默认参数**。
`ENTRYPOINT` 更强调“这个容器启动时一定要执行的主命令”。
如果两者同时存在，通常是 `ENTRYPOINT` 作为主命令，`CMD` 作为默认参数补充给它。
面试里常见说法是：

- `CMD` 更容易被 `docker run` 后面的命令覆盖
- `ENTRYPOINT` 更适合把容器做成一个固定用途的可执行单元

------

### 8. `COPY` 和 `ADD` 的区别是什么？

`COPY` 功能更单纯，就是把本地文件复制到镜像里。
`ADD` 除了复制文件，还支持自动解压本地压缩包，以及从 URL 获取资源。
但工程里通常更推荐优先用 `COPY`，因为语义清晰、行为更可控。
只有确实需要 `ADD` 的额外功能时才使用。

------

### 9. Docker 镜像是怎么分层的？

Docker 镜像由多层只读层叠加组成。
每执行一条可能生成文件系统变化的 Dockerfile 指令，通常就会形成一层新层。
容器启动时，会在镜像只读层上再叠加一个可写层。
这样做的好处是多个镜像可以共享底层相同层，节省存储空间，也方便缓存和分发。

------

### 10. 分层存储有什么好处？

主要有三个好处。
第一，**复用**。多个镜像可以共享相同的基础层。
第二，**构建快**。如果某层没有变化，可以直接复用构建缓存。
第三，**传输高效**。分发镜像时只需要传新增层，不需要每次传整个镜像。
所以分层机制是 Docker 镜像高效构建和分发的核心基础。

------

### 11. 容器的数据如何持久化？

容器本身的可写层不适合长期持久化数据，因为容器删掉后这层数据可能也没了。
常见持久化方式有两种：

- 使用 **volume**
- 使用 **bind mount**

前者是 Docker 管理的数据卷，后者是把宿主机目录直接挂进容器。
数据库、日志、上传文件这类需要长期保留的数据，一般都不会只放在容器可写层里。

------

### 12. 数据卷 volume 是什么？

volume 是 Docker 提供的持久化存储机制，本质上是由 Docker 管理的一块宿主机存储空间。
它可以挂载到容器内目录，供容器读写。
即使容器被删除，volume 里的数据通常仍然保留。
它比直接依赖容器可写层更安全，也更适合做数据持久化和容器间共享数据。

------

### 13. 端口映射是怎么做的？

Docker 通过端口映射把宿主机端口和容器端口关联起来。
常见写法是：

```bash
docker run -p 8080:80 nginx
```

表示访问宿主机 8080 端口时，流量会转发到容器内的 80 端口。
这样外部才能访问容器内服务。
本质上是 Docker 在网络层做了转发和 NAT 相关处理。

------

### 14. 容器之间如何通信？

如果多个容器在同一个 Docker 网络下，它们通常可以直接通过容器名或 IP 通信。
最常见是使用 bridge 网络，Docker 会给容器分配虚拟网卡和 IP。
在 Compose 或自定义网络里，容器之间往往还能直接用服务名互相访问。
所以容器间通信的关键是：**放到同一个网络里**。

------

### 15. Docker 网络模式有哪些？

常见网络模式有：

- `bridge`
- `host`
- `none`

此外还有容器共享网络栈等特殊模式，比如 `container`。
其中最常用的是 `bridge` 和 `host`。

------

### 16. `bridge`、`host`、`none` 模式的区别是什么？

`bridge` 是默认模式。容器有自己的网络命名空间，通过 Docker 虚拟网桥和宿主机通信，需要端口映射对外提供服务。
`host` 模式下，容器直接共享宿主机网络栈，没有独立 IP，也不需要端口映射，性能更直接，但隔离性更弱。
`none` 模式下，容器几乎没有网络能力，适合极特殊场景。
一句话：

- `bridge`：默认、隔离性好
- `host`：网络性能直接、隔离弱
- `none`：无网络

------

### 17. Docker 启动慢或镜像很大，怎么优化？

常见优化思路有：

1. 选更小的基础镜像，比如 alpine 或精简版基础镜像
2. 减少不必要文件进入镜像，配合 `.dockerignore`
3. 合并无意义层，减少镜像层数
4. 把变化少的步骤放前面，提升缓存命中
5. 使用多阶段构建
6. 构建完成后清理编译缓存、临时包

面试里最好强调：**镜像大多数时候不是因为业务代码大，而是因为基础镜像和构建垃圾太多**。

------

### 18. 多阶段构建是什么？

多阶段构建是指在一个 Dockerfile 里使用多个 `FROM`。
前面的阶段用于编译、打包，后面的阶段只拷贝最终产物到一个更干净、更小的运行时镜像里。
这样就避免把编译器、源码、构建缓存都带进最终镜像。
好处是镜像更小、更安全，也更适合生产部署。

------

### 19. 如何进入一个正在运行的容器？

最常用的是：

```bash
docker exec -it container_name /bin/bash
```

如果镜像里没有 bash，也可以用 `/bin/sh`。
`exec` 是在已有容器里启动一个新进程进入交互终端。
一般不推荐老的 `docker attach` 做调试，因为体验和控制都没 `exec` 好。

------

### 20. 如何查看容器日志？

最常用的是：

```bash
docker logs container_name
docker logs -f container_name
```

`-f` 表示持续追踪，类似 `tail -f`。
如果应用日志输出到标准输出和标准错误，Docker 日志就很好看。
这也是为什么很多容器化应用推荐把日志直接打到 stdout/stderr，而不是只写容器内文件。

------

### 21. Docker 容器退出了怎么排查？

常见排查步骤是：

1. `docker ps -a` 看容器退出状态码
2. `docker logs 容器名` 看启动日志和报错
3. 检查启动命令、环境变量、挂载目录、端口占用
4. 必要时临时覆盖启动命令进入容器调试
5. 如果是健康检查失败，也要看 healthcheck 状态

很多容器退出的根因其实很简单，比如主进程跑完了、配置文件没挂进去、依赖服务没连上。

------

### 22. 容器和宿主机文件共享怎么做？

最常见是用挂载。
可以用 bind mount，把宿主机目录直接挂到容器里：

```bash
docker run -v /host/data:/app/data image
```

也可以用 volume。
如果是开发调试场景，经常用 bind mount；
如果是更规范的持久化管理，常用 volume。

------

### 23. Docker Compose 是什么？适合什么场景？

Docker Compose 是用一个 YAML 文件定义和管理多个容器服务的工具。
可以一次性描述服务、网络、卷、环境变量、端口等，然后统一启动。
它特别适合本地开发、多服务联调、小型部署场景。
比如一个项目有 `web + mysql + redis`，用 Compose 就比一条条 `docker run` 管理方便很多。

------

### 24. 容器重启策略有哪些？

常见重启策略有：

- `no`：默认，不自动重启
- `always`：总是自动重启
- `unless-stopped`：除非手动停掉，否则自动重启
- `on-failure`：只在非 0 退出码时重启，可指定次数

生产环境常见的是 `unless-stopped` 或 `always`。
但如果程序本身有严重启动失败问题，单纯自动重启并不能解决根因。

------

### 25. Docker 常见安全问题有哪些？

常见安全问题包括：

1. 容器逃逸
2. 镜像里带有漏洞包或恶意代码
3. 容器以 root 身份运行，权限过大
4. 挂载宿主机敏感目录过多
5. secrets、密码直接写死在镜像或环境变量里
6. 网络暴露面过大

工程上一般会通过最小权限原则、非 root 运行、镜像扫描、只读文件系统、限制 capability 等方式降低风险。

### 26. namespace 和 cgroup 分别是什么？

namespace 负责“隔离”，让容器看起来像有自己独立的进程空间、网络空间、挂载空间等。
cgroup 负责“限制和统计”，控制容器最多能用多少 CPU、内存、IO 等资源。
所以一句话：**namespace 解决看起来分开，cgroup 解决资源别抢太多**。
Docker 的容器能力本质上就是建立在这两套 Linux 机制上。

------

### 27. Docker 如何限制 CPU 和内存资源？

运行容器时可以指定资源限制参数。
比如限制内存：

```bash
docker run -m 512m image
```

限制 CPU：

```bash
docker run --cpus="1.5" image
```

底层本质上就是通过 cgroup 控制资源使用。
这样可以防止某个容器把宿主机资源全部吃满，影响其他服务。

------

### 28. 镜像仓库私有化了解吗？

私有镜像仓库就是企业内部自己搭建或使用私有镜像服务，用来存储和分发内部镜像。
这样做的好处是：

- 镜像不暴露到公网
- 下载更快
- 便于权限控制和版本管理
- 可以接入企业 CI/CD 流程

常见实现有 Harbor 之类的私有仓库方案。
面试里答到“安全、权限、企业内部交付”这几个点就够了。

------

### 29. Docker 和 Kubernetes 的关系是什么？

Docker 主要解决的是“单个应用如何容器化和运行”。
Kubernetes 解决的是“很多容器怎么在集群里统一编排、调度、伸缩、容错和服务发现”。
可以理解成：Docker 更偏容器运行时和打包交付，K8s 更偏大规模容器管理平台。
一句话：Docker 管单个容器，K8s 管一群容器。

------

### 30. 容器编排为什么需要 K8s？

因为当容器数量变多后，手工管理就会很复杂。
比如要解决：

- 哪个节点运行哪个容器
- 挂了怎么自动拉起
- 流量怎么负载均衡
- 怎么滚动升级
- 怎么自动扩缩容
- 服务之间怎么发现彼此

这些都是单纯 Docker 不擅长的大规模治理问题，所以需要 Kubernetes 这类编排系统。

------

### 31. 容器内一个进程挂了会怎么样？

一般情况下，容器的生命周期和它的主进程绑定。
如果主进程退出了，容器通常也就退出了。
如果挂掉的是容器里某个非主进程，要看主进程是否依然存活。
这也是为什么容器设计里通常强调：**主进程要明确，容器最好围绕主进程运行**。

------

### 32. 为什么说一个容器最好只跑一个主进程？

因为这样职责更单一，生命周期更清晰，日志、监控、重启策略都更容易管理。
如果一个容器里塞很多无关进程，就会增加调试难度、耦合度和运维复杂度。
当然“一个容器一个进程”更准确地说是“一个主服务进程”，并不是绝对只能有一个系统级子进程。
它本质上是一种**高内聚、易运维**的设计原则。

------

## 操作系统

### 1. 进程和线程的区别是什么？

进程是资源分配的基本单位，线程是 CPU 调度的基本单位。
一个进程有自己独立的地址空间、文件描述符表等资源；同一进程内的多个线程共享地址空间和大部分进程资源。
线程切换开销通常比进程小，通信也更方便，但一个线程出问题更容易影响整个进程。
一句话：**进程更重、隔离更强；线程更轻、共享更多**。

------

### 2. 线程切换和进程切换哪个开销更大？为什么？

一般来说，进程切换开销更大。
因为进程切换不仅要切换 CPU 上下文，还常常涉及虚拟地址空间切换、页表切换、TLB 失效等。
线程切换通常只需要切换线程上下文，比如寄存器、栈指针、程序计数器等，同进程内线程共享地址空间，所以成本更低。
所以高并发服务内部更常用多线程而不是频繁多进程切换。

------

### 3. 进程间通信有哪些方式？

常见 IPC 方式有：

- 管道 pipe
- 命名管道 FIFO
- 消息队列
- 共享内存
- 信号量
- 信号
- socket
- mmap

如果问性能，一般共享内存最快；
如果问跨机器通信，一般用 socket；
如果是父子进程简单通信，管道很常见。

------

### 4. 线程同步有哪些方式？

常见线程同步方式有：

- 互斥锁 mutex
- 读写锁
- 条件变量
- 信号量
- 原子变量
- 自旋锁
- 屏障

互斥锁主要保护临界区；
条件变量主要做线程间等待和通知；
原子变量适合简单共享状态。
面试里回答时最好顺手加一句“不同同步方式解决的问题不完全一样”。

------

### 5. 什么是互斥？什么是同步？

互斥强调的是**同一时刻只能有一个线程访问某个共享资源**，主要是防止并发冲突。
同步强调的是**线程之间按某种时序协作**，比如一个线程生产数据，另一个线程等到数据准备好再消费。
所以互斥关注“不能同时做”，同步关注“什么时候该做”。
两者经常同时出现，但概念不同。

------

### 6. 什么是死锁？死锁的四个必要条件是什么？

死锁是指多个进程或线程互相等待对方持有的资源，导致谁也无法继续执行。
四个必要条件是：

1. 互斥
2. 请求并保持
3. 不可剥夺
4. 循环等待

只有这四个条件同时满足，才可能发生死锁。
工程里最常见的规避方式是统一加锁顺序，破坏循环等待。

------

### 7. 如何避免死锁？

常见方法有：

- 固定资源申请顺序
- 一次性申请全部资源
- 缩短持锁时间
- 避免嵌套锁
- 使用 `trylock` 或超时机制
- 使用死锁检测工具

最实用的工程做法通常是：**统一锁顺序 + 降低锁粒度 + 减少嵌套加锁**。

------

### 8. 银行家算法了解吗？

银行家算法是一种经典的死锁避免算法。
它在系统分配资源前，先判断这次分配后系统是否仍然处于**安全状态**。
如果会导致系统进入不安全状态，就拒绝这次资源分配。
它更偏理论和教学，在实际工程系统中直接使用不多，但它体现的核心思想是：**分配资源前先判断是否可能走向死锁**。

------

### 9. 什么是上下文切换？

上下文切换是指 CPU 从当前执行实体切换到另一个执行实体时，需要保存当前运行状态，并恢复下一个运行状态的过程。
这里的状态包括寄存器、程序计数器、栈指针、调度信息等。
上下文切换本身不做业务工作，但会消耗 CPU 时间，所以频繁切换会影响性能。
高并发程序里如果线程太多，就可能因为上下文切换过多而变慢。

------

### 10. 用户态和内核态的区别是什么？

用户态权限较低，普通应用程序运行在用户态，不能直接执行特权指令，也不能直接访问硬件。
内核态权限高，操作系统内核运行在内核态，可以管理内存、设备、进程调度等。
当应用程序需要文件读写、网络通信、进程管理等能力时，要通过**系统调用**切换到内核态。
所以用户态和内核态的本质区别是**权限等级不同**。

------

### 11. 什么情况下会发生系统调用？

当用户程序需要内核提供的服务时，就会发生系统调用。
典型场景包括：

- 文件读写
- 网络收发
- 进程创建和销毁
- 内存映射
- 获取时间
- 权限控制

比如 `read`、`write`、`open`、`fork`、`execve` 都属于系统调用。
系统调用会触发从用户态进入内核态，因此有一定开销。

------

### 12. 中断和异常的区别是什么？

中断一般是**外部事件**触发的，比如网卡收到数据、时钟到时、键盘输入。
异常一般是**当前指令执行过程中**发现的问题或特殊情况，比如除零、缺页、非法指令。
两者都会让 CPU 暂停当前执行流程，转去执行内核中的处理程序。
简单记忆就是：**中断更偏外部，异常更偏内部执行触发**。

------

### 13. 虚拟内存是什么？为什么需要虚拟内存？

虚拟内存是操作系统给每个进程提供的一套“看起来连续、独立”的地址空间。
进程访问的是虚拟地址，操作系统和 MMU 再把它映射到物理内存。
需要虚拟内存主要有三个原因：

1. 提供进程间隔离
2. 让程序看到连续地址空间，便于编程
3. 允许按需加载、换页，提高内存利用率

所以虚拟内存既解决了安全隔离问题，也提升了资源利用率。

------

### 14. 分页和分段的区别是什么？

分页是把虚拟地址空间和物理内存都划分成固定大小的页和页框，再通过页表映射。
分段是按逻辑意义划分，比如代码段、数据段、栈段，每段大小可以不同。
分页更适合内存管理和离散分配，但可能有页内碎片；
分段更符合程序逻辑，但容易产生外部碎片。
现代操作系统里通常以分页为主，有些体系结构会结合分段思想使用。

------

### 15. 页表是干什么的？

页表用于把虚拟页号映射到物理页框号。
当 CPU 访问一个虚拟地址时，会先根据虚拟地址中的页号查页表，找到对应物理页，再加上页内偏移得到物理地址。
因为每个进程都有自己的虚拟地址空间，所以通常也有自己的页表。
页表是虚拟内存实现的核心数据结构之一。

------

### 16. 快表 TLB 是什么？

TLB 是 Translation Lookaside Buffer，通常叫快表。
它本质上是一个高速缓存，用来缓存最近使用过的页表映射。
如果每次访问内存都去查页表，会很慢；有了 TLB，常用映射可以直接命中，大幅降低地址转换开销。
所以 TLB 的作用就是：**加速虚拟地址到物理地址的转换**。

------

### 17. 缺页中断是什么？

缺页中断是指进程访问的虚拟页当前没有映射到物理内存，或者不在主存中，于是触发异常，让操作系统来处理。
处理流程一般是：

1. 判断访问是否合法
2. 若合法，找到对应页在磁盘上的位置
3. 分配空闲页框或淘汰旧页
4. 把目标页加载到内存
5. 更新页表，再重新执行导致缺页的那条指令

所以缺页中断本质上是按需调页机制的一部分。

------

### 18. 页面置换算法有哪些？LRU 怎么实现？

常见页面置换算法有：

- FIFO
- LRU
- LFU
- Clock
- OPT

LRU 的思想是淘汰“最近最久未使用”的页，因为它认为未来最不可能再访问。
严格实现 LRU 通常代价较高，一般要借助链表加哈希，或者硬件访问位做近似。
实际操作系统里很多时候不会用完全精确的 LRU，而是用 Clock 等近似算法。

------

### 19. 堆和栈的区别是什么？

栈由系统自动分配和回收，主要存函数调用信息、局部变量、返回地址等。
堆由程序员或运行时动态申请和释放，适合生命周期不固定、大小动态变化的对象。
栈分配释放快，但空间相对小；堆空间大，但管理成本高，也更容易产生碎片和内存泄漏。
一句话：**栈偏自动和高效，堆偏灵活和动态**。

------

### 20. 内存泄漏和内存溢出的区别是什么？

内存泄漏是指程序申请的内存没有被释放，并且之后也无法再访问到，导致这部分内存永久丢失。
内存溢出是指程序申请内存时，系统已经没有足够可用内存来满足请求，导致申请失败甚至程序崩溃。
前者更像“内存一点点丢了”，后者更像“内存已经不够用了”。
长期内存泄漏往往最终会导致内存溢出。

------

### 21. 什么是缓冲区溢出？

缓冲区溢出是指向一块固定大小的缓冲区写入了超过它容量的数据，覆盖了相邻内存。
这可能导致程序崩溃、数据损坏，严重时还会被利用进行攻击，比如覆盖返回地址执行恶意代码。
C/C++ 由于允许直接操作内存，所以这类问题更常见。
常见防护手段有边界检查、安全函数、栈保护、ASLR 等。

------

### 22. 什么是零拷贝？

零拷贝的目标是减少数据在内核态和用户态之间，以及内存缓冲区之间的不必要复制。
比如文件传输场景下，如果每次都从磁盘读到内核，再拷到用户态，再拷回内核发给网卡，开销就很大。
零拷贝通过 `sendfile`、`mmap`、`splice` 等机制，减少这些中间复制和上下文切换。
这样可以提升吞吐、降低 CPU 消耗。

------

### 23. DMA 是什么？

DMA 是 Direct Memory Access，直接内存访问。
它允许外设在不频繁打扰 CPU 的情况下，直接和内存交换数据。
比如网卡、磁盘控制器可以通过 DMA 把数据直接搬到内存。
这样 CPU 不需要逐字节参与复制，效率更高。
所以 DMA 是高性能 IO 的重要基础。

------

### 24. 什么是文件描述符？

文件描述符是操作系统为进程维护的一个非负整数，用来标识一个打开的文件、socket、管道等内核对象。
在 Unix/Linux 里，“一切皆文件”，所以很多资源都通过文件描述符统一抽象。
进程进行读写、网络通信时，通常都是拿着文件描述符去调用系统调用。
标准输入、输出、错误默认分别对应 0、1、2。

------

### 25. 从输入 URL 到页面显示，中间涉及哪些 OS 层面的内容？

从 OS 角度可以这样回答：

1. 浏览器发起 DNS 查询和网络请求，需要系统调用进入内核
2. 内核通过网络协议栈收发数据包
3. 网卡通过中断和 DMA 与内存交互
4. 收到响应数据后，内核把数据交给用户态浏览器进程
5. 浏览器进程/线程解析 HTML、CSS、JS
6. 期间涉及多线程调度、内存管理、文件缓存、socket IO 等

也就是说，这个过程背后会用到**系统调用、进程线程调度、内存管理、网络协议栈、中断机制**等多个 OS 知识点。

------

### 26. 什么是系统调用？常见系统调用有哪些？

系统调用是用户程序向操作系统内核申请服务的接口。
因为用户态权限不够，很多事情必须通过系统调用让内核代为完成。
常见系统调用有：

- `open`
- `read`
- `write`
- `close`
- `fork`
- `execve`
- `wait`
- `mmap`
- `socket`
- `accept`

一句话：系统调用就是**用户态访问内核能力的标准入口**。

------

### 27. 阻塞和非阻塞的区别是什么？

阻塞是指调用一个操作时，如果条件不满足，线程会一直等待直到完成。
非阻塞是指条件不满足时，立即返回，不会让线程卡住。
比如 socket 读操作中，阻塞 IO 没数据就睡眠等待；非阻塞 IO 没数据就立刻返回 `EAGAIN`。
所以区别在于：**调用方是否会被挂起等待**。

------

### 28. 同步和异步的区别是什么？

同步强调的是调用发起后，调用方要等结果回来再继续后续逻辑。
异步强调的是调用发起后，不必原地等待，结果准备好后再通过回调、通知、事件等方式处理。
很多人容易把“同步/异步”和“阻塞/非阻塞”混淆。
更准确地说：

- 阻塞/非阻塞描述的是等待时线程状态
- 同步/异步描述的是结果通知方式

------

### 29. select、poll、epoll 本质上属于什么模型？

它们本质上都属于 **IO 多路复用模型**。
核心思想是一个线程同时监听多个文件描述符，当某些 fd 就绪时再进行处理。
这样就避免为每个连接都单独创建线程阻塞等待。
所以它们是高并发网络服务器常见的 IO 模型基础。

------

### 30. 为什么高并发服务端常用 epoll？

因为在高并发连接场景下，`epoll` 相比 `select/poll` 更高效。
它不需要每次把整个 fd 集合从用户态拷到内核态，也不需要每次遍历全部 fd。
`epoll_wait` 返回的是已经就绪的事件集合，所以当连接数很多而活跃连接较少时优势尤其明显。
这也是 Linux 下高性能网络服务器常常基于 epoll 的原因。

### 31. 什么是惊群问题？

惊群问题是指多个进程或线程同时等待同一个事件，当事件到来时，大家一起被唤醒，但最终只有一个真正获得资源，其余又白白切回去睡眠。
这样会造成大量无效唤醒和上下文切换。
它常出现在早期多进程监听同一个 socket、锁竞争或条件变量设计不当的场景里。
现代系统会通过更合理的事件通知机制减少惊群问题。

------

### 32. 写时拷贝 COW 是什么？

COW 是 Copy-On-Write。
它的思想是：多个实体先共享同一份数据，只有当某一方真的要修改时，才复制出一份新的。
在操作系统里，`fork` 后父子进程一开始通常共享相同的物理页，只有某一方写入时才触发页面复制。
这样能显著减少不必要的内存复制，提高性能。

------

### 33. fork 之后父子进程共享什么，不共享什么？

`fork` 之后，父子进程从逻辑上看拥有各自独立的地址空间，但很多页面一开始会通过 COW 共享物理内存。
它们共享的通常包括代码段内容、打开文件的内核对象、部分内核状态等。
不共享的是各自的进程 ID、独立的虚拟地址空间视图、页表、副本后的用户态数据修改结果等。
简单说：**初始内容很多看起来一样，但本质上是两个独立进程，写时才真正分开**。

------

### 34. mmap 和 read/write 的区别是什么？

`read/write` 是显式地把数据从内核缓冲区拷到用户缓冲区，再由程序处理。
`mmap` 是把文件或设备映射到进程虚拟地址空间，程序像访问内存一样访问文件内容。
`mmap` 在大文件访问、随机访问、共享内存场景里很有优势，也常能减少拷贝次数。
但它也有页错误、映射管理等复杂性。
一句话：`read/write` 更直接通用，`mmap` 更适合高效映射和共享。

------

### 35. 文件系统了解多少？

面试里可以答：
文件系统负责把磁盘这种块设备组织成目录、文件、inode、数据块等结构，对上提供统一的文件访问接口。
它要解决数据如何存、如何查、如何回收、如何保证一致性等问题。
常见组成包括超级块、inode 区、数据块区、目录项。
工程里最常接触的是权限、inode、日志恢复、page cache 和挂载点这些概念。

------

### 36. ext4 的特点是什么？

ext4 是 Linux 中常见的本地文件系统之一。
它相对 ext3 支持更大的文件和文件系统、更高效的块管理、延迟分配、extent、日志功能等。
它性能稳定、成熟度高、兼容性好，所以在很多 Linux 发行版中长期作为默认文件系统。
面试里说出“**日志、extent、成熟稳定**”这几个关键词通常就够了。

------

### 37. 从磁盘读取一个文件到用户态，发生了什么？

大致流程可以这样说：

1. 用户进程发起 `read` 系统调用
2. CPU 从用户态切换到内核态
3. 内核检查页缓存里是否已有数据
4. 如果没有，就发起磁盘 IO
5. 磁盘控制器通过 DMA 把数据搬到内存
6. 触发中断通知内核 IO 完成
7. 内核把数据拷到用户缓冲区
8. 返回用户态，程序继续执行

这个过程会涉及系统调用、页缓存、DMA、中断、内核态切换等多个知识点。

------

### 38. CPU 调度算法有哪些？

常见调度算法有：

- 先来先服务 FCFS
- 短作业优先 SJF
- 时间片轮转 RR
- 优先级调度
- 多级反馈队列 MLFQ

现代通用操作系统一般不会只用一种，而是综合考虑响应时间、吞吐量、公平性等因素。
Linux 的实际调度器实现比教材算法更复杂，但教材里最常考的还是这些经典算法。

------

