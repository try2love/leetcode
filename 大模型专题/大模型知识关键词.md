关键词即题单，遇见一个词要立马想起来它对应的相关知识，并且能够和其他知识串联起来。

## 大语言模型本体相关

- [ ] tokenizer
- [ ] embedding
- [ ] 位置编码
- [ ] Attention机制：self-attention, cross attention, multi-head attention, multi-query attention, group query attention, flash attention, paged attention (VLLM), Sparse Attention , Linear Attention, deep seek-v2 Multi-head Latent Attention, KV Cache, Sliding Window Attention SWA, Sliding Window with Dilation Attention(SwDA)
- [ ] 残差连接
- [ ] 归一化
- [ ] Encoder-decoder, encoder only, decoder only
- [ ] 激活函数
- [ ] FFN前馈神经网络
- [ ] MOE
- [ ] 自回归训练
  - [ ] 自监督 无监督等方案
  - [ ] Mask机制
  - [ ] 梯度消失
  - [ ] 梯度爆炸
  - [ ] 交叉熵，KL散度，JS散度，最大似然，交叉熵对logits的导数
  - [ ] PPL
  - [ ] 一阶动量，二阶动能RMSProp
  - [ ] 优化器Adam，AdamW，Weight Decay
  - [ ] warmup
  - [ ] Cosine decay
  - [ ] deepspeed（zero三阶段、3D并行）
- [ ] 自回归生成
  - [ ] temperature超参数
  - [ ] top-k采样
  - [ ] top-p采样
  - [ ] beam search
  - [ ] superlative decode
  - [ ] deepseek-v3 Multi-Token Prediction
- [ ] 模型量化
  - [ ] FP32， FP16， BF16， INT8， INT4
  - [ ] 混合精度
  - [ ] 对称量化，非对称量化
  - [ ] PTQ，GPTQ，AWQ
- [ ] 模型微调
  - [ ] chat template
  - [ ] SFT，数据集的构建
  - [ ] RM
  - [ ] PPO
  - [ ] DPO
  - [ ] deepseek-GRPO
  - [ ] LoRA及其变体

## 大语言模型应用-RAG

- [ ] 知识库
  - [ ] chunk切分策略
  - [ ] ANN索引
  - [ ] FAISS/HNSW构建索引库
- [ ] 检索器
  - [ ] embedding model（对比学习等）
  - [ ] dense retriever
  - [ ] sparse retriever
  - [ ] 评估指标：recall@k，latency
- [ ] Reranker
  - [ ] 重排序策略
- [ ] 生成器
  - [ ] Query Rewrite
  - [ ] Multi-Query
  - [ ] HyDE
  - [ ] 评估指标
  - [ ] 幻觉归因
  - [ ] citation grounding
- [ ] Naive RAG
- [ ] Self-RAG
- [ ] GraphRAG
- [ ] Langchain

## 大模型应用-Agent

- [ ] Function Calling
- [ ] Tool Use
- [ ] Skills
- [ ] planning
  - [ ] ReACT
  - [ ] Plan-and-Execute
  - [ ] Tree-of-Thought
- [ ] Memory
- [ ] Self-check
- [ ] MCP

# 自省

## 2026-03-17尝试复现

- [x] tokenizer：word piece, character piece, subseqence piexe(BPE, ) **遗忘**一个删除似然影响最小的，一个新加入提高似然的还有一个用unicode编码的，但是忘记了名称。
- [x] embedding：word embedding, sentence embedding - RAG, token id -> embedding 映射, 需要训练，常用对比学习，正样本和副样本的构建：batch内负样本，随机采样副样本（？，改写、同义词替换等正样本？

- [x] **遗忘**embedding对比学习的正负样本构建、遗忘常见的对比学习策略，只能想起来名称比如infoNCE，simNCE，但是不知道具体的实现策略

- [x] 位置编码：transformers中的sin、cos位置编码 ->  RoPE -> YaRN。这里应该暂时不考虑attention变更策略后怎么加入位置编码信息了。 **遗忘**： YaRN对那些不放缩，对哪里放缩，哪里线性放缩？

- [x] Attention：**遗忘**：online softmax的原理、Linear Attention的定义和原理，是核函数吗？滑动窗口类型attenion的原理遗忘

- [x] 残差链接与归一化：**遗忘**：PreNorm和PostNorm的区别；batchnorm和layernorm的比较和为什么选它的解释不够好，只想起来了序列长度变长+小batch的原因。

- [x] 模型架构：**遗忘**：不同结构擅长的事情，尤其是encoder-decoder擅长什么

- [x] 困惑：decoder only架构的prefill阶段，没办法让最开始的token注意到输入内容的后续token吗？如果不是的话，sparse attention存在的意义是什么？明明之后当前最后的token会和前向token计算attention，前向对所有的后项都是0注意力啊，哪里来的on2呢。在我的认知里对大模型的注意力应该是和GLM架构一致，输入信息之间是全部互相注意，自回归生成只对前向有注意力。

- [x] 激活函数：sigmoid，tanh，ReLU，αReLU，LeakyReLU。其他的**按顺序遗忘**。GLU是负部分是指数-1吗？silu的原理？这些都忘掉了

- [x] FFN：引入非线性变换，往往是d->4d->d，对于SwiGLU d->8/3d -> d

- [x] MoE：把FFN进行切分，每一个token走若干个专家头。router，topk专家，共享专家

- [x] 自监督、无监督：**遗忘**：定义以及区别。自监督回归任务，无监督聚类任务。

- [x] mask机制：训练时并行加速。困惑：模型在推理的时候有用到mask机制吗？

- [x] 梯度消失与梯度爆炸的原因和解决方案：**遗忘**：梯度消失的解决方案只能想起来激活函数、残差链接、梯度裁剪。梯度爆炸解决方案只能想起来更好的初始化、梯度裁剪方案。

- [x] 熵：**遗忘**哪一些有负号有哪些没有负号，KL散度没有负号

- [x] PPL：**遗忘**公式。困惑：PPL和序列长度有关吗？

- [x] 一阶动量和二阶动能：**遗忘**计算公式

- [x] 优化器：**遗忘**计算公式

- [x] warmup：只能想起来线性递增

- [x] decay方案：遗忘其他的decay，只能想起来cos，线性、step

- [x] deepspeed：遗忘它切分的是那些部分了，划分成为哪三部分。切分后，信息如何汇总？如何更新模型？

- [x] 困惑：数据并行阶段，如果梯度信息汇聚到第一张卡上，那么输入信息呢？激活值信息呢？需要汇聚到第一张卡上吗？DDP分布式数据并行的流程遗忘。张量并行的定义遗忘

- [x] topk采样做的事情是选取前k个最大概率的元素，然后等概率随机选择吗？topp是选概率最高的若干几个，高于p之后就不选择了，然后从这些选定结果中等概率随机选择一个token作为采样结果

- [x] beamsearch强调的是链路上的最大似然吗？投机采样大模型接受小模型的采样结果，这里的判定依据是什么？遗忘了。MTP是一个训练策略，那有考虑用在生成中吗？

- [x] 混合精度训练到底指的是什么？是模型用fp32，梯度fp32，adamfp32，激活fp16这种吗？混合精度推理呢？有没有这一说？（AWQ就是的吧，大的激活值就用fp32）

- [x] 完全遗忘对称量化和非对称量化

- [ ] 遗忘GPTQ，有印象的是嗨森矩阵？这个原理没有学明白

- [ ] SFT的策略是什么？难道只是一个简单的交叉熵吗？如果是的话，那不会产生一个灾难性遗忘吗？

- [ ] 完全不知道Reward Model是如何构建出来的，难道是让SFT针对一个问题输出一组，然后组内排序？这不是DPO吗？dpo就是不需要reward model和critical model的策略啊。

- [ ] PPO的损失函数没有印象，DPO的损失也没有印象，但是和PPO挺相似的，消除了A(t)奖励机制

- [ ] GRPO方案目前只知道相比于PPO，删去了critical model，但是内部细节和损失设置完全不知道。

- [ ] LoRA，Adaptive LoRA，LoRA+，PISSA，等策略，但是对不上号，不知道哪个名称的具体策略，比如奇异值分解是PISSA的吗？LoRA+应该是学习率不同？对不上号了。

- [ ] 

- [ ] 现在的chunk策略是什么+overlap？段落级别、句子级别、token长度级别？

- [ ] ANN的中文意义是什么？除了HNSW之外，其他的ANN策略是什么

- [ ] 知识库构建全流程：

- [ ] 1. 数据收集，爬虫、购买等方案
  2. 数据清洗，尤其要清洗掉各种无用头尾信息，比如copyrightxxx，html各种标签等
  3. 数据去重，现在常用的策略是minHash，另一个策略名称忘记了
  4. chunk 切分（疑问：如果graphRAG，在哪里构建图？如果新增数据，怎么添加图网络？）
  5. embedding model转为embedding，存储到知识库中

- [ ] 疑惑：如果说默认使用cos相似度，那么对于百万级别数据库，一次性返回的结果是不是就太多了，因为cos是先归一化，再计算点积的，球体就那么大，每一个样本都被映射到球体的面上。

- [ ] 检索器评估指标里面的latency是什么？

- [ ] reranker只能想起来LLM based策略。

- [ ] query rewrite也只能想起来大模型的方案

## 2026-04-13尝试

- [ ] 使用tokenizer实现分词（遗忘较多）
  - [ ] tokenizer的类型包括：word based，character based，BPE（全称忘记了），BBPE，还有一个策略遗忘了，其中关于似然的有两个构建策略，一个是把能够最大似然的合并，一个是对似然影响最小的删除。
- [ ] embedding layer VS embedding model
  - [ ] 前者是Transformer架构中的一层，把token转embedding，训练方式：通常是对比学习，InfoNCE的损失函数遗忘，只能想起来log，分子正例分母全部 - log分子负例，分母全部？
  - [ ] 后者是一个独立的模型，实现把句子映射到高维度空间中，训练方式主要还是对比学习。通常情况下采取的措施是，取model的最后一层输出的hidden state作为句子的高维表示，那么这个过程中，如何使用对比学习？遗忘：给定一个base model，如何训练出来一个embedding model
- [ ] 注意力机制的计算
  - [ ] self Attention, Multi-Head Attention, Multi-Query Attention, Group-Query Attention, FlashAttention (Online Softmax遗忘核心策略), vLLM PagedAttention, Linear Attention, MLA(全称忘记了，好像是Multi-Latent Attention?), Sparse Attention, SWA（滑动窗口）, SwDA（qwen系列的滑动窗口）
- [ ] VLLM的其他策略，例如高效批处理
- [ ] Post-Norm， Pre-Norm, LayerNorm, RMSNorm, BatchNorm
- [ ] FFN前馈神经网络
  - [ ] 激活函数：ReLU, LeakReLU, alphaReLU, tanh, sigmoid, GLU, Swish，后面遗忘，从门控开始就忘记公式了
  - [ ] 升维后降维，常规是4D，像qwen系列使用SwiGLU之后，是8/3维度
- [ ] 梯度消失的措施
  - [ ] 落到激活函数的饱和区 -> 换激活函数（最关键）
  - [ ] 网络深度太深，反向传播导致前向层梯度消失 -> 残差链接
  - [ ] 梯度裁剪，确保下边界的存在
- [ ] 梯度爆炸的措施
  - [ ] 梯度裁剪，确保上边界的存在（最关键）
  - [ ] warmup措施
  - [ ] 更好的参数初始化
- [ ] 采样：
  - [ ] 温度：对logits进行操作，实现放缩
  - [ ] topk：取logits最大的k个，其余置为-inf
  - [ ] topp：对softmax后得到的概率分布，取前若干个直到和大于等于p，其余置为-inf，再来一遍softmax
  - [ ] beam search：保留似然估计最大的若干个采样链路
  - [ ] Superlative Decoding（中文名忘记了）
- [ ] MoE：
  - [ ] 路由，共享专家，专属专家，在Vllm中，如果一个专家分配token到上线了怎么办？
- [ ] 自回归训练
  - [ ] 梯度消失梯度爆炸，略
  - [ ] teacher forcing、causal masking、交叉熵、KL散度、JS散度、最大似然、交叉熵对logits的梯度
  - [ ] Deepspeek Multi Token Prediciton训练
  - [ ] PPL
  - [ ] 一阶动量，二阶动能RMSProp
  - [ ] Adam优化器，AdamW优化器，SDG
  - [ ] warmup，cos decay
  - [ ] deepspeed三阶段
  - [ ] llamafactory微调
- [ ] 模型量化
  - [ ] 对称量化
  - [ ] 非对称量化
  - [ ] int8量化
  - [ ] QloRA的int4量化
  - [ ] 混合精度
  - [ ] PTQ，AWQ，GPTQ（遗忘）
- [ ] 后训练
  - [ ] SFT
  - [ ] RLHF
  - [ ] RM
  - [ ] Critic Model
  - [ ] PPO
  - [ ] DPO
  - [ ] deepspeek-GRPO
  - [ ] LoRA变体