# RAG系统评估指标



# 知识库构建

- 数据采集
- 结构化的文档比如表格，代码，FAQ怎么分段？
- 数据清洗（乱码，重复，模板噪声等）
- 数据去重怎么做？simhash/minihash/embedding去重之间有什么差异？
- 如何保留位置信息（doc_ic/section_id/span）用于引用？
- 如何确定数据的版本（保证最新）
- 多版本冲突怎么处理？
- 如何界定不同用户对知识库的访问权限，比如一些敏感数据只能某些用户访问？如何做审计和可追溯？
- 文档切分 Chunking，切分太大或者太小的后果？
- overlap解决了什么又带来了什么？
- 什么是元数据，元数据有什么用
- 知识库构建阶段把内容转embedding，使用的embedding model如何选择？
- 假设使用FAISS稠密向量库，如何把文档存入？
- 如何确保原始文档能通过向量反向还原/映射
- 存入向量的可选策略有哪些？比如我们项目中使用了faiss.IndexFlatL2用于创建索引
- 搜索最相似的文档的策略有哪些？比如我们使用的是faiss.read_index得到index，对index执行search，但是不知道具体的检索策略

# 检索器

- 稀疏检索器BM25，k1和b这两个参数如何影响结果
- 稠密检索器是什么？如何在应用中权衡？如何使用自己的数据训练/微调？
- 相似度计算方案有dot和cos，有什么区别，如何选额
- 稠密检索器的高效方案：ANN
- HNSW是什么，有什么参数，如何调参
- IVF- PQ或者SQ这种量化索引是什么？成本和内存问题？
- PQ为什么省内存，代价是什么？召回为什么掉，如何补救？
- 检索到了相关文档仍然回答错误的原因分析
- embedding模型如何选择？维度，归一化，向量漂移如何处理？可以对base embedding model做什么处理？

# 重排器

- 什么是重排器，如何实现重排，需要重排的文档从哪里来
- rerank放入多少候选合适？top50？top200？如何选择？
- 重排器为什么有效，代价是什么，如何平衡有效性和代价
- cross-encoder，LLM rerank，Learning to rank
- cross-encoder为什么比双塔强？代价是什么？
- rerank结果不稳定怎么处理？（投票/温度/固定提示/去偏）
- 有冲突文档怎么办

# 用户输入Query

- 查询重写策略为什么很有必要
- query拓展策略，Query Rewrite, Multi-Query, HyDE
- 为什么rewrite有时会让召回变差，怎么做gating？
- query路由策略如何做？为什么要路由机制？怎么训练/实现router？如何避免router错分导致全盘崩溃？真的错分了怎么都低？
- 多跳问题如何处理？会引入什么风险？如何防止发散？终止条件是什么？怎么控制步数和预算？
- 复杂问题如何拆分子问题，怎么验证每一步的正确性？
- 如何处理用户问题不清晰？

# prompt聚合

- 如何把topk文档拼成最终的context？（去重、排序、预算控制）
- 提示词模板是什么？设计依据是什么？
- 如何做结构化输出（json schema），失败怎么修复？
- 为什么不把所有检索结果塞入prompt，或者使用微调策略替代RAG
- 数据去重，如何实现段落级别的去重并作冲突策略？
- 多个版本的事实都被检索到了，怎么处理？
- 上下文压缩怎么做，如何避免压缩信息丢失？怎么评估压缩质量？
- 为什么压缩更可能产生幻觉？如何做引用对齐避免“摘要幻觉”
- 如何处理指代不明的问题（指代不明），尤其是针对多轮对话
- 为什么模型会忽略证据？怎么让模型做到证据优先？
- 如何做指令隔离防止PIA？

# 生成结果

- 如何做citation grounding引用对齐？对齐做错了怎么办？如何做生成后校验和回退？
- 如何规定输出格式
- 如何判断拒绝回答的边界？拒答太多怎么办，如何降低误据？
- 如何尽可能减小幻觉，哪怕说我不知道都比幻觉强
- 如何处理敏感信息泄漏？

# RAG架构

- Naive RAG，朴素RAG
- Advanced RAG，高级RAG
- Modular RAG，模块化RAG
- Iterative RAG，迭代式RAG
- Hierarchical RAG，分层RAG
- Adaptive RAG，自适应RAG
- GraphRAG，LightRAG
- Agentic RAG，智能体RAG

# 工程落地上线应用

- 缓存策略缓存什么？如何实现RAG系统的批处理？
- P50，P95，P99延迟代表什么？cache hit率是什么？
- 缓存带来什么一致性问题（文档更新但是缓存没更新）？怎么做缓存的实效策略？
- 如何实现文档的增量更新？包括增删和版本控制
- 文档更新后索引如何重构？HNSW删除很难怎么办？（墓碑/重建/分票）
- 数据漂移是什么？如何监控这一现象？有什么解决方案？
- 如何做模块的开关和降级？比如中间对质量有关的reranker挂掉了怎么办？
- 有做过哪些测试，结论是什么？（各种权衡）
- 给你一个效果下降的线上警报，排查顺序是什么
- 延迟飙升但是QPS没变，可能的原因是什么？（缓存击穿/索引碎片/重排超时）
- 某类query大量拒绝回答，怎么排错？（router/拒答阈值调整/分布漂移）
- 引用错位怎么修复？（chunk id， span， post-check）